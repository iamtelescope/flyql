all: test
fmt:
	go fmt ./...
	cd e2e && go fmt ./...
fmt-test:
	@test -z "$$(gofmt -l .)" || (echo "Files not formatted:" && gofmt -l . && exit 1)
vet:
	go vet ./...
lint:
	golangci-lint run ./...
gotest:
	go test -v ./...
test: fmt-test vet gotest
coverage:
	go test -coverprofile=coverage.out ./...
	go tool cover -html=coverage.out -o coverage.html
	go tool cover -func=coverage.out
cleanup:
	go clean -testcache
