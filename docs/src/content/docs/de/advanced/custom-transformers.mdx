---
title: Benutzerdefinierte Transformers
description: Registriere domänenspezifische Transformer, um FlyQL mit eigenen Werttransformationen zu erweitern
---

import { Tabs, TabItem } from '@astrojs/starlight/components'

Das Transformer-System von FlyQL ist erweiterbar. Du kannst benutzerdefinierte Transformer registrieren, die sich in Parsern, SQL-Generatoren, In-Memory-Matchern und der Editor-Autovervollständigung genauso verhalten wie eingebaute Transformer (`upper`, `lower`, `len`).

## Transformer-Interface

Jeder Transformer implementiert 6 Elemente:

| Element | Typ | Beschreibung |
|---------|-----|--------------|
| `name` | string | Eindeutiger Bezeichner in Kleinbuchstaben (z. B. `"firstoctet"`) |
| `inputType` | Type | Erwarteter Eingabetyp (`string`, `int`, `float`, `bool`, `array` oder `any` für einen Wildcard-Eingang, der jeden Upstream-Typ akzeptiert — siehe [Any-Input-Transformer](#any-input-transformer)) |
| `outputType` | Type | Ausgabetyp nach der Transformation |
| `argSchema` | ArgSpec[] | Deklarierte Argumenttypen und -anzahl (Standard: leer = keine Argumente) |
| `sql(dialect, columnRef, args)` | string | Erzeugt dialektspezifisches SQL, das die Spaltenreferenz umschließt |
| `apply(value, args)` | any | Transformiert einen Laufzeitwert im Speicher |

Die Methode `sql()` erhält den SQL-Dialektnamen (`"clickhouse"`, `"postgresql"`, `"starrocks"`), eine Zeichenkette mit der Spaltenreferenz und die geparste Argumentliste. Sie gibt einen SQL-Ausdruck zurück, der diese Referenz umschließt. Die Methode `apply()` erhält einen Laufzeitwert und die Argumentliste, und gibt den transformierten Wert zurück.

### ArgSpec

Jeder Eintrag in `argSchema` deklariert ein erwartetes Argument:

| Feld | Typ | Standard | Beschreibung |
|------|-----|----------|--------------|
| `type` | Type | — | Erwarteter Argumenttyp (`string`, `int`, `float`, `bool`) |
| `required` | boolean | `true` | Ob das Argument angegeben werden muss |

Die Arität ergibt sich aus der Schema-Länge: Pflichtargumente müssen vorhanden sein, optionale Argumente am Ende können weggelassen werden. Transformer ohne Argumente (wie `upper`, `lower`, `len`) verwenden das leere Standard-Schema.

## Registry-API

| Methode | Beschreibung |
|---------|--------------|
| `defaultRegistry()` | Gibt eine neue Registry mit bereits geladenen eingebauten Transformern zurück |
| `registry.register(transformer)` | Fügt einen benutzerdefinierten Transformer hinzu (Fehler, wenn der Name bereits registriert ist) |
| `registry.get(name)` | Sucht einen Transformer per Name (liefert null/None/nil, wenn nicht gefunden) |
| `registry.names()` | Listet alle registrierten Transformer-Namen auf |

Das typische Muster: Standard-Registry holen, benutzerdefinierte Transformer registrieren, Registry an Generatoren und Matcher übergeben.

## Python

Definiere einen benutzerdefinierten Transformer, indem du `Transformer` erweiterst:

```python
from flyql.transformers import Transformer, Type, default_registry

class FirstOctetTransformer(Transformer):
    @property
    def name(self): return "firstoctet"
    @property
    def input_type(self): return Type.STRING
    @property
    def output_type(self): return Type.INT
    def sql(self, dialect, column_ref, args=None):
        if dialect == "clickhouse":
            return f"toUInt8(splitByChar('.', {column_ref})[1])"
        return f"CAST(SPLIT_PART({column_ref}, '.', 1) AS INTEGER)"
    def apply(self, value, args=None):
        return int(str(value).split('.')[0])
```

Das Klassenattribut `arg_schema` hat den Standardwert `()` (keine Argumente). Um Argumente zu deklarieren, überschreiben Sie es mit einem Tupel von `ArgSpec`:

```python
from typing import ClassVar, Tuple
from flyql.transformers.base import ArgSpec

class SubstringTransformer(Transformer):
    arg_schema: ClassVar[Tuple[ArgSpec, ...]] = (
        ArgSpec(type=Type.INT),                    # start (required)
        ArgSpec(type=Type.INT, required=False),    # length (optional)
    )
    # ... name, input_type, output_type, sql, apply ...
```

Registrieren und verwenden:

```python
from flyql import parse
from flyql.generators.postgresql.generator import to_sql_where, Column

registry = default_registry()
registry.register(FirstOctetTransformer())

result = parse("src_ip|firstoctet > 192")
columns = {"src_ip": Column("src_ip", "text")}
sql = to_sql_where(result.root, columns, registry=registry)
# CAST(SPLIT_PART(src_ip, '.', 1) AS INTEGER) > 192
```

Für In-Memory-Matching:

```python
from flyql.matcher.evaluator import Evaluator
from flyql.matcher.record import Record

evaluator = Evaluator(registry=registry)
record = Record({"src_ip": "193.0.0.1"})
evaluator.evaluate(result.root, record)  # True (193 > 192)
```

## JavaScript

Definiere einen benutzerdefinierten Transformer, indem du `Transformer` erweiterst:

```javascript
import { Transformer, Type, ArgSpec, defaultRegistry } from 'flyql/transformers'

class FirstOctetTransformer extends Transformer {
    get name() { return 'firstoctet' }
    get inputType() { return Type.STRING }
    get outputType() { return Type.INT }
    sql(dialect, columnRef, args = []) {
        if (dialect === 'clickhouse') return `toUInt8(splitByChar('.', ${columnRef})[1])`
        return `CAST(SPLIT_PART(${columnRef}, '.', 1) AS INTEGER)`
    }
    apply(value, args = []) { return parseInt(String(value).split('.')[0], 10) }
}
```

Der `argSchema` Getter hat den Standardwert `[]` (keine Argumente). Um Argumente zu deklarieren, überschreiben Sie ihn:

```javascript
class SubstringTransformer extends Transformer {
    get argSchema() {
        return [
            new ArgSpec(Type.INT, true),     // start (required)
            new ArgSpec(Type.INT, false),    // length (optional)
        ]
    }
    // ... name, inputType, outputType, sql, apply ...
}
```

Registrieren und verwenden:

```javascript
import { parse } from 'flyql'
import { generateWhere, newColumn } from 'flyql/generators/postgresql'

const registry = defaultRegistry()
registry.register(new FirstOctetTransformer())

const result = parse("src_ip|firstoctet > 192")
const columns = { src_ip: newColumn({ name: 'src_ip', type: 'text' }) }
const sql = generateWhere(result.root, columns, registry)
// CAST(SPLIT_PART(src_ip, '.', 1) AS INTEGER) > 192
```

Für In-Memory-Matching:

```javascript
import { match } from 'flyql/matcher'

match("src_ip|firstoctet > 192", { src_ip: "193.0.0.1" }, registry) // true
```

Für die Editor-Autovervollständigung erscheinen benutzerdefinierte Transformer neben den eingebauten:

```javascript
import { EditorEngine } from 'flyql-vue'

const engine = new EditorEngine(columns, { registry })
// After typing "src_ip|", suggestions include "firstoctet", "upper", "lower", "len"
```

## Go

Definiere einen benutzerdefinierten Transformer, indem du das `Transformer`-Interface implementierst:

```go
package main

import (
    "fmt"
    "strconv"
    "strings"
    "github.com/iamtelescope/flyql/golang/transformers"
)

type FirstOctet struct{}

func (f FirstOctet) Name() string               { return "firstoctet" }
func (f FirstOctet) InputType() transformers.Type  { return transformers.TransformerTypeString }
func (f FirstOctet) OutputType() transformers.Type { return transformers.TransformerTypeInt }
func (f FirstOctet) ArgSchema() []transformers.ArgSpec        { return []transformers.ArgSpec{} }
func (f FirstOctet) SQL(dialect, colRef string, args []any) string {
    if dialect == "clickhouse" {
        return fmt.Sprintf("toUInt8(splitByChar('.', %s)[1])", colRef)
    }
    return fmt.Sprintf("CAST(SPLIT_PART(%s, '.', 1) AS INTEGER)", colRef)
}
func (f FirstOctet) Apply(value interface{}, args []any) interface{} {
    parts := strings.SplitN(fmt.Sprint(value), ".", 2)
    n, _ := strconv.Atoi(parts[0])
    return n
}
```

Um Argumente zu deklarieren, geben Sie `ArgSpec`-Einträge aus `ArgSchema()` zurück:

```go
func (s Substring) ArgSchema() []transformers.ArgSpec {
    return []transformers.ArgSpec{
        {Type: transformers.TransformerTypeInt, Required: true},   // start (required)
        {Type: transformers.TransformerTypeInt, Required: false},  // length (optional)
    }
}
```

Registrieren und verwenden:

```go
import (
    flyql "github.com/iamtelescope/flyql/golang"
    postgresqlgen "github.com/iamtelescope/flyql/golang/generators/postgresql"
)

registry := transformers.DefaultRegistry()
registry.Register(FirstOctet{})

result, _ := flyql.Parse("src_ip|firstoctet > 192")
columns := map[string]*postgresqlgen.Column{
    "src_ip": postgresqlgen.NewColumn("src_ip", "text", nil),
}
sql, _ := postgresqlgen.ToSQLWhere(result.Root, columns, registry)
// CAST(SPLIT_PART(src_ip, '.', 1) AS INTEGER) > 192
```

Für In-Memory-Matching:

```go
import "github.com/iamtelescope/flyql/golang/matcher"

matched, _ := matcher.Match("src_ip|firstoctet > 192", map[string]any{"src_ip": "193.0.0.1"}, registry)
// matched == true
```

## Any-Input-Transformer

Ein Transformer kann `inputType = Type.Any` (String-Wert `"any"`) deklarieren, um jeden Upstream-Spalten- oder Transformer-Ausgabetyp zu akzeptieren. Der motivierende Anwendungsfall sind typunabhängige Operationen — zum Beispiel ein `type`-Transformer, der den Typnamen der Spalte zurückgibt, unabhängig davon, um welchen Typ es sich handelt.

Regeln:

- `Any` ist **nur** auf `inputType` gültig. Das Deklarieren von `Any` als `outputType` oder auf einem beliebigen `ArgSpec.type` wird zur `register()`-Zeit mit einem gezielten Fehler abgelehnt. Jeder Transformer muss weiterhin einen konkreten Ausgabetyp deklarieren.
- Wenn ein Any-Input-Transformer in einer Kette verwendet wird, wird die Eingabetyp-Gleichheitsprüfung für diesen Schritt übersprungen. Die Downstream-Striktheit bleibt erhalten — z. B. `col|accepts_any_returning_array|upper` schlägt weiterhin bei `upper` fehl, weil `array` keine gültige Eingabe für `upper` ist.
- `parseFlyQLType("any")` löst einen Fehler aus: `any` ist kein gültiger Spalten- oder Werttyp.

<Tabs syncKey="lang">
<TabItem label="Python">

```python
from flyql.flyql_type import Type
from flyql.transformers import Transformer

class TypeTransformer(Transformer):
    @property
    def name(self): return "type"
    @property
    def input_type(self): return Type.Any      # akzeptiert jeden Upstream-Typ
    @property
    def output_type(self): return Type.String  # muss konkret sein
    def sql(self, dialect, column_ref, args=None):
        if dialect == "clickhouse":
            return f"toTypeName({column_ref})"
        return f"pg_typeof({column_ref})::text"
    def apply(self, value, args=None):
        return type(value).__name__
```

</TabItem>
<TabItem label="JavaScript">

```javascript
import { Type } from 'flyql'
import { Transformer } from 'flyql/transformers'

class TypeTransformer extends Transformer {
    get name() { return 'type' }
    get inputType() { return Type.Any }         // akzeptiert jeden Upstream-Typ
    get outputType() { return Type.String }     // muss konkret sein
    sql(dialect, columnRef) {
        if (dialect === 'clickhouse') return `toTypeName(${columnRef})`
        return `pg_typeof(${columnRef})::text`
    }
    apply(value) {
        return typeof value
    }
}
```

</TabItem>
<TabItem label="Go">

```go
import (
    "fmt"
    "reflect"
    "github.com/iamtelescope/flyql/golang/flyqltype"
    "github.com/iamtelescope/flyql/golang/transformers"
)

type TypeTransformer struct{}

func (TypeTransformer) Name() string                      { return "type" }
func (TypeTransformer) Description() string               { return "" }
func (TypeTransformer) InputType() flyqltype.Type         { return flyqltype.Any }     // Wildcard
func (TypeTransformer) OutputType() flyqltype.Type        { return flyqltype.String }  // konkret
func (TypeTransformer) ArgSchema() []transformers.ArgSpec { return nil }
func (TypeTransformer) SQL(dialect, columnRef string, args []any) string {
    if dialect == "clickhouse" {
        return fmt.Sprintf("toTypeName(%s)", columnRef)
    }
    return fmt.Sprintf("pg_typeof(%s)::text", columnRef)
}
func (TypeTransformer) Apply(value any, args []any) any {
    if value == nil {
        return "nil"
    }
    return reflect.TypeOf(value).String()
}
```

</TabItem>
</Tabs>

## Typkompatibilität

Benutzerdefinierte Transformer nehmen an derselben Typketten-Validierung teil wie eingebaute. Wenn du `field|firstoctet|upper` verkettst, erkennt das System, dass `firstoctet` einen `int` ausgibt, `upper` aber einen `string` als Eingabe benötigt.

Verfügbare Typen: siehe [`flyql.Type`](/advanced/column-types/). Die einzige Ausnahme ist `Any`, was ein Transformer-Input-Wildcard und kein gültiger Spalten- oder Werttyp ist.

## Validierung mit `diagnose()`

Die Funktion `diagnose()` durchläuft einen geparsten AST gegen ein Spaltenschema und eine Transformer-Registry und gibt positionierte `Diagnostic`-Einträge zurück, ohne Exceptions auszulösen. Sie prüft:

- **Unbekannte Spalte** — Basisschlüssel nicht in der Spaltenliste
- **Unbekannter Transformer** — Transformer-Name nicht in der Registry
- **Falsche Arität** — zu wenige oder zu viele Argumente gegenüber `argSchema`
- **Falscher Argumenttyp** — Werttyp des Arguments stimmt nicht mit dem deklarierten `ArgSpec`-Typ überein (int-zu-float-Erweiterung ist erlaubt)
- **Kettentyp-Konflikt** — Ausgabetyp eines Transformers stimmt nicht mit dem Eingabetyp des nächsten überein, oder der Spaltentyp stimmt nicht mit dem Eingabetyp des ersten Transformers überein

Jede Diagnose enthält einen `range` (Quellposition), `message`, `severity` (`"error"`) und `code` (`"unknown_column"`, `"unknown_transformer"`, `"arg_count"`, `"arg_type"`, `"chain_type"`).

```python
# Python
from flyql import parse, Column, ColumnSchema, Type, diagnose

ast = parse("hstt|upper=X").root
columns = ColumnSchema.from_columns([Column(name="host", column_type=Type.String)])
diags = diagnose(ast, columns)  # columns is a ColumnSchema
# [Diagnostic(range=Range(0, 4), message="column 'hstt' is not defined", severity='error', code='unknown_column')]
```

```javascript
// JavaScript
import { parse, Column, ColumnSchema, Type, diagnose } from 'flyql'

const ast = parse('hstt|upper=X').root
const columns = ColumnSchema.fromColumns([new Column('host', false, Type.String)])
const diags = diagnose(ast, columns)  // columns is a ColumnSchema
// [Diagnostic { range: Range(0, 4), message: "column 'hstt' is not defined", severity: 'error', code: 'unknown_column' }]
```

```go
// Go
import flyql "github.com/iamtelescope/flyql/golang"

result, _ := flyql.Parse("hstt|upper=X")
schema := flyql.FromColumns([]flyql.Column{flyql.NewColumn("host", flyql.TypeString)})
diags := flyql.Diagnose(result.Root, schema, nil) // nil = default registry
// []Diagnostic{{Range: {0, 4}, Message: "column 'hstt' is not defined", Severity: "error", Code: "unknown_column"}}
```

Übergib eine benutzerdefinierte Registry als drittes Argument, wenn du benutzerdefinierte Transformers registriert hast. Übergib `nil`/`None`/`null`, um die Standard-Registry zu verwenden.

## Sicherheit

Die `sql()`-Methoden benutzerdefinierter Transformer erhalten nur Spaltenreferenzen aus dem Schema, niemals benutzerdefinierte Werte. Benutzerwerte werden vom Generator escaped bzw. parametrisiert. Damit bleibt die Injektionssicherheits-Garantie von FlyQL erhalten – benutzerdefiniertes SQL umschließt Spaltennamen, nicht Benutzereingaben.

## Siehe auch

- [Benutzerdefinierte Renderers](/advanced/custom-renderers/) — registriere
  Post-Alias-Anzeige-Metadaten-Deskriptoren. Im Gegensatz zu Transformers
  beeinflussen Renderers niemals das generierte SQL; sie beschreiben nur, wie
  deine Anwendung einen Wert darstellen soll.
