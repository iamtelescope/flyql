---
title: StarRocks
description: Beispiele für generiertes SQL in StarRocks
---

import FlyqlToken from '../../../../components/FlyqlToken.astro'
import SqlExamplesTable from '../../../../components/SqlExamplesTable.astro'
import { rows as basicExamples } from '../../../../snippets/sql/starrocks/basic-examples.js'
import { rows as booleanOperators } from '../../../../snippets/sql/starrocks/boolean-operators.js'
import { rows as listMembership } from '../../../../snippets/sql/starrocks/list-membership.js'
import { rows as containment } from '../../../../snippets/sql/starrocks/containment.js'
import { rows as jsonNative } from '../../../../snippets/sql/starrocks/json-native.js'
import { rows as jsonString } from '../../../../snippets/sql/starrocks/json-string.js'
import { rows as mapColumns } from '../../../../snippets/sql/starrocks/map-columns.js'
import { rows as arrayColumns } from '../../../../snippets/sql/starrocks/array-columns.js'
import { rows as transformers } from '../../../../snippets/sql/starrocks/transformers.js'

StarRocks-SQL verwendet mit Backticks quotierte Bezeichner, `regexp()` für Regex und `->` mit JSON-stringifizierten Schlüsseln für JSON-Zugriffe.

## Grundlegende Beispiele

<SqlExamplesTable rows={basicExamples} />

## Boolesche Operatoren

<SqlExamplesTable rows={booleanOperators} />

## Listenzugehörigkeit

<SqlExamplesTable rows={listMembership} />

## Enthalten (`has` / `not has`)

Der Operator <FlyqlToken type="operator">has</FlyqlToken> erzeugt je nach Spaltentyp unterschiedliches SQL:

<SqlExamplesTable
    rows={containment}
    metaHeader="Spaltentyp"
    metaLabels={["String", "String (negiert)", "Array", "Array (negiert)", "Map", "Map (negiert)", "Natives JSON"]}
/>

## JSON-Spalten

StarRocks unterstützt zwei Typen von JSON-Spalten mit unterschiedlicher SQL-Ausgabe.

### Nativer JSON-Typ

Native JSON-Spalten verwenden den `->`-Operator mit JSON-stringifizierten Schlüsseln:

<SqlExamplesTable rows={jsonNative} />

### JSON-String-Typ

Spalten, die JSON als String speichern, benötigen vor der Traversierung `parse_json()`:

<SqlExamplesTable rows={jsonString} />

## Verschachtelte komplexe Typen

Der StarRocks-Generator behandelt einen segmentierten Pfad als zu einem einzelnen Containertyp gehörend — er steigt derzeit nicht durch gemischte Verschachtelungen wie `Map(K, Struct)`, `Array(Map)` oder `Struct`-Felder, die selbst `Map`/`Array` sind, ab. Für solche Spalten den Pfad abflachen oder den inneren Wert als eigene Spalte bereitstellen.

## Map-Spalten

Map-Spalten verwenden Klammernotation mit direktem Vergleich:

<SqlExamplesTable rows={mapColumns} />

## Array-Spalten

FlyQL-Array-Indizes sind 0-basiert; der StarRocks-Generator addiert beim Erzeugen des SQL 1, weil Arrays in StarRocks auf der SQL-Ebene 1-basiert sind:

<SqlExamplesTable rows={arrayColumns} />

### Spaltenkonfiguration

{/* TODO: native-speaker review */}
Text/Varchar-Spalten mit JSON-Inhalt werden wie in ClickHouse mit dem synthetischen Rohtyp `jsonstring` deklariert:

```go
// Go
columns := map[string]*starrocks.Column{
    "message": starrocks.NewColumn(starrocks.ColumnDef{Name: "message", Type: "VARCHAR(255)"}),
    "data":    starrocks.NewColumn(starrocks.ColumnDef{Name: "data", Type: "JSON"}),      // native JSON
    "log":     starrocks.NewColumn(starrocks.ColumnDef{Name: "log", Type: "jsonstring"}),       // JSON string
}
```

```javascript
// JavaScript
const columns = {
    message: newColumn({ name: "message", type: "VARCHAR(255)" }),
    data:    newColumn({ name: "data", type: "JSON" }),      // native JSON
    log:     newColumn({ name: "log", type: "jsonstring" }),       // JSON string
}
```

## Transformers

Transformers erzeugen StarRocks-Standardfunktionsaufrufe mit in Backticks quotierten Spaltennamen:

<SqlExamplesTable rows={transformers} />
