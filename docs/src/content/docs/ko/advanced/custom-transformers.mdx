---
title: 사용자 정의 Transformers
description: 도메인별 transformer를 등록해 사용자 정의 값 변환으로 FlyQL을 확장합니다
---

import { Tabs, TabItem } from '@astrojs/starlight/components'

FlyQL의 transformer 시스템은 확장 가능합니다. parser, SQL generator, in-memory matcher, editor autocomplete 전반에서 내장 transformer(`upper`, `lower`, `len`)와 동일하게 동작하는 사용자 정의 transformer를 등록할 수 있습니다.

## Transformer 인터페이스

모든 transformer는 6개의 멤버를 구현합니다.

| 멤버 | 타입 | 설명 |
|--------|------|-------------|
| `name` | string | 고유한 소문자 식별자입니다. 예: `"firstoctet"` |
| `inputType` | Type | 기대하는 입력 타입입니다. (`string`, `int`, `float`, `bool`, `array`), 또는 모든 상위 타입을 받는 와일드카드 입력인 `any` — [Any 입력 transformer](#any-입력-transformer) 참조 |
| `outputType` | Type | 변환 후 출력 타입입니다. |
| `argSchema` | ArgSpec[] | 선언된 인수 타입 및 인수 개수 (기본값: 비어 있음 = 인수 없음) |
| `sql(dialect, columnRef, args)` | string | column 참조를 감싸는 방언별 SQL을 생성합니다. |
| `apply(value, args)` | any | 런타임에서 메모리 내 값을 변환합니다. |

`sql()` 메서드는 SQL dialect 이름(`"clickhouse"`, `"postgresql"`, `"starrocks"`), column 참조 문자열, 파싱된 인수 목록을 받습니다. 이 메서드는 해당 참조를 감싸는 SQL expression을 반환합니다. `apply()` 메서드는 런타임 값과 인수 목록을 받아 변환된 값을 반환합니다.

### ArgSpec

`argSchema`의 각 항목은 기대하는 인수 하나를 선언합니다.

| 필드 | 타입 | 기본값 | 설명 |
|------|------|--------|------|
| `type` | Type | — | 기대하는 인수 타입 (`string`, `int`, `float`, `bool`) |
| `required` | boolean | `true` | 인수를 반드시 제공해야 하는지 여부 |

인수 개수(arity)는 스키마 길이에서 파생됩니다. 필수 인수는 반드시 있어야 하고, 끝에 있는 선택적 인수는 생략할 수 있습니다. 인수가 없는 transformer(`upper`, `lower`, `len` 등)는 기본 빈 스키마를 상속합니다.

## Registry API

| 메서드 | 설명 |
|--------|-------------|
| `defaultRegistry()` | 내장 transformer가 미리 로드된 새 registry를 반환합니다. |
| `registry.register(transformer)` | 사용자 정의 transformer를 추가합니다. 이름이 이미 등록되어 있으면 에러가 발생합니다. |
| `registry.get(name)` | 이름으로 transformer를 조회합니다. 찾지 못하면 null/None/nil을 반환합니다. |
| `registry.names()` | 등록된 모든 transformer 이름을 나열합니다. |

패턴은 다음과 같습니다. 기본 registry를 가져오고, 사용자 정의 transformer를 등록한 뒤, 그 registry를 generator와 matcher에 전달합니다.

## Python

`Transformer`를 상속해 사용자 정의 transformer를 정의합니다.

```python
from flyql.transformers import Transformer, Type, default_registry

class FirstOctetTransformer(Transformer):
    @property
    def name(self): return "firstoctet"
    @property
    def input_type(self): return Type.STRING
    @property
    def output_type(self): return Type.INT
    def sql(self, dialect, column_ref, args=None):
        if dialect == "clickhouse":
            return f"toUInt8(splitByChar('.', {column_ref})[1])"
        return f"CAST(SPLIT_PART({column_ref}, '.', 1) AS INTEGER)"
    def apply(self, value, args=None):
        return int(str(value).split('.')[0])
```

`arg_schema` 클래스 속성의 기본값은 `()`(인수 없음)입니다. 인수를 선언하려면 `ArgSpec` 튜플로 오버라이드하세요:

```python
from typing import ClassVar, Tuple
from flyql.transformers.base import ArgSpec

class SubstringTransformer(Transformer):
    arg_schema: ClassVar[Tuple[ArgSpec, ...]] = (
        ArgSpec(type=Type.INT),                    # start (required)
        ArgSpec(type=Type.INT, required=False),    # length (optional)
    )
    # ... name, input_type, output_type, sql, apply ...
```

등록하고 사용하기:

```python
from flyql import parse
from flyql.generators.postgresql.generator import to_sql_where, Column

registry = default_registry()
registry.register(FirstOctetTransformer())

result = parse("src_ip|firstoctet > 192")
columns = {"src_ip": Column("src_ip", "text")}
sql = to_sql_where(result.root, columns, registry=registry)
# CAST(SPLIT_PART(src_ip, '.', 1) AS INTEGER) > 192
```

in-memory matching:

```python
from flyql.matcher.evaluator import Evaluator
from flyql.matcher.record import Record

evaluator = Evaluator(registry=registry)
record = Record({"src_ip": "193.0.0.1"})
evaluator.evaluate(result.root, record)  # True (193 > 192)
```

## JavaScript

`Transformer`를 확장해 사용자 정의 transformer를 정의합니다.

```javascript
import { Transformer, Type, ArgSpec, defaultRegistry } from 'flyql/transformers'

class FirstOctetTransformer extends Transformer {
    get name() { return 'firstoctet' }
    get inputType() { return Type.STRING }
    get outputType() { return Type.INT }
    sql(dialect, columnRef, args = []) {
        if (dialect === 'clickhouse') return `toUInt8(splitByChar('.', ${columnRef})[1])`
        return `CAST(SPLIT_PART(${columnRef}, '.', 1) AS INTEGER)`
    }
    apply(value, args = []) { return parseInt(String(value).split('.')[0], 10) }
}
```

`argSchema` getter의 기본값은 `[]`(인수 없음)입니다. 인수를 선언하려면 오버라이드하세요:

```javascript
class SubstringTransformer extends Transformer {
    get argSchema() {
        return [
            new ArgSpec(Type.INT, true),     // start (required)
            new ArgSpec(Type.INT, false),    // length (optional)
        ]
    }
    // ... name, inputType, outputType, sql, apply ...
}
```

등록하고 사용하기:

```javascript
import { parse } from 'flyql'
import { generateWhere, newColumn } from 'flyql/generators/postgresql'

const registry = defaultRegistry()
registry.register(new FirstOctetTransformer())

const result = parse("src_ip|firstoctet > 192")
const columns = { src_ip: newColumn({ name: 'src_ip', type: 'text' }) }
const sql = generateWhere(result.root, columns, registry)
// CAST(SPLIT_PART(src_ip, '.', 1) AS INTEGER) > 192
```

in-memory matching:

```javascript
import { match } from 'flyql/matcher'

match("src_ip|firstoctet > 192", { src_ip: "193.0.0.1" }, registry) // true
```

editor autocomplete:

```javascript
import { EditorEngine } from 'flyql-vue'

const engine = new EditorEngine(columns, { registry })
// "src_ip|"를 입력하면 "firstoctet", "upper", "lower", "len" 제안이 함께 표시됩니다
```

## Go

`Transformer` 인터페이스를 구현해 사용자 정의 transformer를 정의합니다.

```go
package main

import (
    "fmt"
    "strconv"
    "strings"
    "github.com/iamtelescope/flyql/golang/transformers"
)

type FirstOctet struct{}

func (f FirstOctet) Name() string               { return "firstoctet" }
func (f FirstOctet) InputType() transformers.Type  { return transformers.TransformerTypeString }
func (f FirstOctet) OutputType() transformers.Type { return transformers.TransformerTypeInt }
func (f FirstOctet) ArgSchema() []transformers.ArgSpec        { return []transformers.ArgSpec{} }
func (f FirstOctet) SQL(dialect, colRef string, args []any) string {
    if dialect == "clickhouse" {
        return fmt.Sprintf("toUInt8(splitByChar('.', %s)[1])", colRef)
    }
    return fmt.Sprintf("CAST(SPLIT_PART(%s, '.', 1) AS INTEGER)", colRef)
}
func (f FirstOctet) Apply(value interface{}, args []any) interface{} {
    parts := strings.SplitN(fmt.Sprint(value), ".", 2)
    n, _ := strconv.Atoi(parts[0])
    return n
}
```

인수를 선언하려면 `ArgSchema()`에서 `ArgSpec` 항목을 반환하세요:

```go
func (s Substring) ArgSchema() []transformers.ArgSpec {
    return []transformers.ArgSpec{
        {Type: transformers.TransformerTypeInt, Required: true},   // start (required)
        {Type: transformers.TransformerTypeInt, Required: false},  // length (optional)
    }
}
```

등록하고 사용하기:

```go
import (
    flyql "github.com/iamtelescope/flyql/golang"
    postgresqlgen "github.com/iamtelescope/flyql/golang/generators/postgresql"
)

registry := transformers.DefaultRegistry()
registry.Register(FirstOctet{})

result, _ := flyql.Parse("src_ip|firstoctet > 192")
columns := map[string]*postgresqlgen.Column{
    "src_ip": postgresqlgen.NewColumn("src_ip", "text", nil),
}
sql, _ := postgresqlgen.ToSQLWhere(result.Root, columns, registry)
// CAST(SPLIT_PART(src_ip, '.', 1) AS INTEGER) > 192
```

in-memory matching:

```go
import "github.com/iamtelescope/flyql/golang/matcher"

matched, _ := matcher.Match("src_ip|firstoctet > 192", map[string]any{"src_ip": "193.0.0.1"}, registry)
// matched == true
```

## Any 입력 transformer

transformer는 `inputType = Type.Any` (문자열 값 `"any"`)를 선언하여 모든 상위 타입을 입력으로 받을 수 있습니다. 대표적인 사용 사례는 컬럼의 타입 이름을 반환하는 `type` transformer입니다.

규칙:

- `Any`는 `inputType`에서만 유효합니다. `outputType`이나 `ArgSpec.type`에 `Any`를 선언하면 `register()` 시점에 거부됩니다. 모든 transformer는 반드시 구체적인 출력 타입을 선언해야 합니다.
- Any 입력 transformer가 체인에서 사용될 때 입력 타입 동등성 검사는 건너뜁니다. 하류 엄격성은 그대로 유지됩니다 — 예: `col|accepts_any_returning_array|upper`는 `array`가 `upper`의 유효한 입력이 아니므로 `upper`에서 여전히 에러가 발생합니다.
- `parseFlyQLType("any")`는 에러를 발생시킵니다. `any`는 유효한 컬럼 또는 값 타입이 아닙니다.

<Tabs syncKey="lang">
<TabItem label="Python">

```python
from flyql.flyql_type import Type
from flyql.transformers import Transformer

class TypeTransformer(Transformer):
    @property
    def name(self): return "type"
    @property
    def input_type(self): return Type.Any      # 모든 상위 타입 수용
    @property
    def output_type(self): return Type.String  # 반드시 구체적이어야 함
    def sql(self, dialect, column_ref, args=None):
        if dialect == "clickhouse":
            return f"toTypeName({column_ref})"
        return f"pg_typeof({column_ref})::text"
    def apply(self, value, args=None):
        return type(value).__name__
```

</TabItem>
<TabItem label="JavaScript">

```javascript
import { Type } from 'flyql'
import { Transformer } from 'flyql/transformers'

class TypeTransformer extends Transformer {
    get name() { return 'type' }
    get inputType() { return Type.Any }         // 모든 상위 타입 수용
    get outputType() { return Type.String }     // 반드시 구체적이어야 함
    sql(dialect, columnRef) {
        if (dialect === 'clickhouse') return `toTypeName(${columnRef})`
        return `pg_typeof(${columnRef})::text`
    }
    apply(value) {
        return typeof value
    }
}
```

</TabItem>
<TabItem label="Go">

```go
import (
    "fmt"
    "reflect"
    "github.com/iamtelescope/flyql/golang/flyqltype"
    "github.com/iamtelescope/flyql/golang/transformers"
)

type TypeTransformer struct{}

func (TypeTransformer) Name() string                      { return "type" }
func (TypeTransformer) Description() string               { return "" }
func (TypeTransformer) InputType() flyqltype.Type         { return flyqltype.Any }     // 와일드카드
func (TypeTransformer) OutputType() flyqltype.Type        { return flyqltype.String }  // 구체적
func (TypeTransformer) ArgSchema() []transformers.ArgSpec { return nil }
func (TypeTransformer) SQL(dialect, columnRef string, args []any) string {
    if dialect == "clickhouse" {
        return fmt.Sprintf("toTypeName(%s)", columnRef)
    }
    return fmt.Sprintf("pg_typeof(%s)::text", columnRef)
}
func (TypeTransformer) Apply(value any, args []any) any {
    if value == nil {
        return "nil"
    }
    return reflect.TypeOf(value).String()
}
```

</TabItem>
</Tabs>

## 타입 호환성

사용자 정의 transformer도 내장 transformer와 동일한 타입 체인 검증에 참여합니다. 예를 들어 `field|firstoctet|upper`를 체인으로 연결하면, 시스템은 `firstoctet`의 출력이 `int`인데 `upper`는 `string`을 요구한다는 점을 감지합니다.

[`flyql.Type`](/advanced/column-types/) 참조. 유일한 예외는 `Any`로, 이는 transformer 입력 와일드카드이며 유효한 컬럼 또는 값 타입이 아닙니다.

## `diagnose()`를 사용한 검증

`diagnose()` 함수는 파싱된 AST를 컬럼 스키마와 transformer 레지스트리에 대해 순회하며, 예외를 발생시키지 않고 위치 정보가 포함된 `Diagnostic` 레코드를 반환합니다. 다음을 검사합니다.

- **알 수 없는 컬럼** — 기본 키가 컬럼 목록에 없음
- **알 수 없는 transformer** — transformer 이름이 레지스트리에 없음
- **잘못된 인수 개수** — `argSchema` 대비 인수가 너무 적거나 많음
- **잘못된 인수 타입** — 인수 값 타입이 선언된 `ArgSpec` 타입과 일치하지 않음 (int에서 float으로의 확장은 허용)
- **체인 타입 불일치** — 한 transformer의 출력 타입이 다음 transformer의 입력 타입과 일치하지 않거나, 컬럼의 정규화된 타입이 첫 번째 transformer의 입력 타입과 일치하지 않음

각 diagnostic은 `range`(소스 위치), `message`, `severity`(`"error"`), `code`(`"unknown_column"`, `"unknown_transformer"`, `"arg_count"`, `"arg_type"`, `"chain_type"`)를 포함합니다.

```python
# Python
from flyql import parse, Column, ColumnSchema, Type, diagnose

ast = parse("hstt|upper=X").root
columns = ColumnSchema.from_columns([Column(name="host", column_type=Type.String)])
diags = diagnose(ast, columns)  # columns는 ColumnSchema입니다
# [Diagnostic(range=Range(0, 4), message="column 'hstt' is not defined", severity='error', code='unknown_column')]
```

```javascript
// JavaScript
import { parse, Column, ColumnSchema, Type, diagnose } from 'flyql'

const ast = parse('hstt|upper=X').root
const columns = ColumnSchema.fromColumns([new Column('host', false, Type.String)])
const diags = diagnose(ast, columns)  // columns는 ColumnSchema입니다
// [Diagnostic { range: Range(0, 4), message: "column 'hstt' is not defined", severity: 'error', code: 'unknown_column' }]
```

```go
// Go
import flyql "github.com/iamtelescope/flyql/golang"

result, _ := flyql.Parse("hstt|upper=X")
schema := flyql.FromColumns([]flyql.Column{flyql.NewColumn("host", flyql.TypeString)})
diags := flyql.Diagnose(result.Root, schema, nil) // nil = 기본 레지스트리
// []Diagnostic{{Range: {0, 4}, Message: "column 'hstt' is not defined", Severity: "error", Code: "unknown_column"}}
```

사용자 정의 transformer를 등록한 경우 세 번째 인수로 사용자 정의 레지스트리를 전달하세요. 기본 레지스트리를 사용하려면 `nil`/`None`/`null`을 전달합니다.

## 보안

사용자 정의 transformer의 `sql()` 메서드는 user 제공 값이 아니라 schema에서 온 column 참조만 받습니다. user 값은 generator가 escape/parameterize 합니다. 이 구조 덕분에 FlyQL의 인젝션 방지 보장은 그대로 유지됩니다. 사용자 정의 SQL은 user 입력이 아니라 column 이름을 감쌉니다.

## 관련 항목

- [Custom Renderers](/advanced/custom-renderers/) — 별칭 뒤 표시 메타데이터 기술자를 등록합니다. transformer와 달리 renderers는 생성되는 SQL에 전혀 영향을 주지 않습니다. 애플리케이션이 값을 어떻게 렌더링해야 하는지만 기술합니다.
