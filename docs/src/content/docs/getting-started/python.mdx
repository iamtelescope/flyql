---
title: Python Quickstart
description: Install and use FlyQL in a Python project
---

import { Code } from '@astrojs/starlight/components'
import parseSnippet from '@snippets/python/getting_started_parse.py?raw'
import generateSnippet from '@snippets/python/getting_started_generate_clickhouse.py?raw'
import matchSnippet from '@snippets/python/getting_started_match.py?raw'
import columnsSnippet from '@snippets/python/getting_started_columns.py?raw'
import transformersSnippet from '@snippets/python/getting_started_transformers.py?raw'

## Installation

FlyQL for Python is installed from git (not published to PyPI):

```bash
pip install "flyql @ git+https://github.com/iamtelescope/flyql.git#subdirectory=python"
```

This installs the `flyql` package along with its dependency `google-re2`.

**Requirements:** Python 3.10+

## Parse a Query

<Code code={parseSnippet.trim()} lang="python" />

`parse()` returns a `Parser` object with a `root` node representing the AST. It raises a `ParserError` on invalid input.

## Generate SQL

Use a generator to turn the AST into a WHERE clause. Each database dialect has its own generator.

### ClickHouse

<Code code={generateSnippet.trim()} lang="python" />

PostgreSQL and StarRocks generators follow the same pattern — import from `flyql.generators.postgresql` or `flyql.generators.starrocks` instead.

## Match In-Memory

Evaluate a query against a data record without generating SQL:

<Code code={matchSnippet.trim()} lang="python" />

## Parse Column Expressions

Parse a columns expression into structured data with segments, transformers, and aliases:

<Code code={columnsSnippet.trim()} lang="python" />

## Transformers in Queries

Parse a query with transformers and generate SQL:

<Code code={transformersSnippet.trim()} lang="python" />

See [Transformers](/syntax/transformers/) for the full syntax reference.

## What's Next

- [Syntax Reference](/syntax/) — all operators, boolean logic, patterns, and more
