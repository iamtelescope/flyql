---
title: ClickHouse
description: Exemples de SQL genere pour ClickHouse
---

import FlyqlToken from '../../../../components/FlyqlToken.astro'
import SqlExamplesTable from '../../../../components/SqlExamplesTable.astro'
import { rows as basicExamples } from '../../../../snippets/sql/clickhouse/basic-examples.js'
import { rows as booleanOperators } from '../../../../snippets/sql/clickhouse/boolean-operators.js'
import { rows as listMembership } from '../../../../snippets/sql/clickhouse/list-membership.js'
import { rows as containment } from '../../../../snippets/sql/clickhouse/containment.js'
import { rows as jsonNative } from '../../../../snippets/sql/clickhouse/json-native.js'
import { rows as jsonString } from '../../../../snippets/sql/clickhouse/json-string.js'
import { rows as mapColumns } from '../../../../snippets/sql/clickhouse/map-columns.js'
import { rows as arrayColumns } from '../../../../snippets/sql/clickhouse/array-columns.js'
import { rows as transformers } from '../../../../snippets/sql/clickhouse/transformers.js'

Le SQL ClickHouse utilise des noms de colonnes sans guillemets, `match()` pour les regex et `LIKE` pour les jokers.

## Exemples de base

<SqlExamplesTable rows={basicExamples} />

## Operateurs booleens

<SqlExamplesTable rows={booleanOperators} />

## Appartenance a une liste

<SqlExamplesTable rows={listMembership} />

## Inclusion (`has` / `not has`)

L'operateur <FlyqlToken type="operator">has</FlyqlToken> genere un SQL different selon le type de colonne :

<SqlExamplesTable
    rows={containment}
    metaHeader="Type de colonne"
    metaLabels={["String", "String (inverse)", "Array", "Array (inverse)", "Map", "Map (inverse)", "JSON natif", "JSON chaine"]}
/>

## Colonnes JSON

ClickHouse supporte deux types de colonnes JSON avec des sorties SQL differentes.

### Type JSON natif

Les colonnes declarees comme `JSON` utilisent la notation par points avec des segments entre backticks :

<SqlExamplesTable rows={jsonNative} />

### Type JSON chaine

Les colonnes stockant du JSON sous forme de chaine (par ex. type `String` avec du contenu JSON) utilisent les fonctions `JSONExtract` enveloppees dans `multiIf` pour la verification de type :

<SqlExamplesTable rows={jsonString} />

## Colonnes Map

Les colonnes Map utilisent la notation entre crochets avec les fonctions `equals()`/`notEquals()` :

<SqlExamplesTable rows={mapColumns} />

## Colonnes Array

Les indices de tableau FlyQL sont en base 0 ; le generateur ClickHouse ajoute 1 lors de l'emission du SQL parce que les tableaux ClickHouse sont indexes a partir de 1 au niveau SQL :

<SqlExamplesTable rows={arrayColumns} />

### Configuration des colonnes

{/* TODO: native-speaker review */}
Les colonnes text/varchar dont le contenu est du JSON valide sont declarees avec le type brut synthetique `jsonstring` :

```go
// Go
columns := map[string]*clickhouse.Column{
    "message": clickhouse.NewColumn(clickhouse.ColumnDef{Name: "message", Type: "String"}),
    "data":    clickhouse.NewColumn(clickhouse.ColumnDef{Name: "data", Type: "JSON"}),      // native JSON
    "log":     clickhouse.NewColumn(clickhouse.ColumnDef{Name: "log", Type: "jsonstring"}),       // JSON string
}
```

```javascript
// JavaScript
const columns = {
    message: newColumn({ name: "message", type: "String" }),
    data:    newColumn({ name: "data", type: "JSON" }),      // native JSON
    log:     newColumn({ name: "log", type: "jsonstring" }),       // JSON string
}
```

## Transformateurs

Les transformateurs generent des appels de fonctions specifiques a ClickHouse :

<SqlExamplesTable rows={transformers} />
