---
title: Go Quickstart
description: Install and use FlyQL in a Go project
---

import { Code } from '@astrojs/starlight/components'
import parseSnippet from '@snippets/go/parse/main.go?raw'
import generateSnippet from '@snippets/go/generate_clickhouse/main.go?raw'
import matchSnippet from '@snippets/go/match/main.go?raw'
import columnsSnippet from '@snippets/go/columns/main.go?raw'
import transformersSnippet from '@snippets/go/transformers/main.go?raw'
import parametersSnippet from '@snippets/go/parameters/main.go?raw'

## Installation

```bash
go get github.com/iamtelescope/flyql/golang
```

## Parse a Query

<Code code={parseSnippet.trim()} lang="go" />

`Parse` returns a `*ParseResult` with a `Root` node representing the AST.

## Generate SQL

Use a generator to turn the AST into a WHERE clause. Each database dialect has its own generator with dialect-specific column types.

### ClickHouse

<Code code={generateSnippet.trim()} lang="go" />

PostgreSQL and StarRocks generators follow the same pattern — import from `generators/postgresql` or `generators/starrocks` instead.

## Match In-Memory

Evaluate a query against a data record without generating SQL:

<Code code={matchSnippet.trim()} lang="go" />

## Parse Column Expressions

Parse a columns expression into structured data with segments, transformers, and aliases.
The second argument is a `Capabilities` struct that controls which features are enabled.
Transformers are disabled by default.

<Code code={columnsSnippet.trim()} lang="go" />

## Parameterized Queries

Use `$name` and `$1` placeholders, then resolve them at runtime with `BindParams()`:

<Code code={parametersSnippet.trim()} lang="go" />

See [Parameters](/syntax/parameters/) for the full reference.

## Transformers in Queries

Parse a query with transformers and generate SQL:

<Code code={transformersSnippet.trim()} lang="go" />

See [Transformers](/syntax/transformers/) for the full syntax reference.

## What's Next

- [Syntax Reference](/syntax/) — all operators, boolean logic, patterns, and more
