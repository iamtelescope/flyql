---
title: 構文概要
description: FlyQL の基本的なクエリ構造と構文ルール
---

import FlyqlCode from '../../../../components/FlyqlCode.astro'
import FlyqlToken from '../../../../components/FlyqlToken.astro'

FlyQL のクエリは、1 つ以上の条件を <FlyqlToken type="operator">and</FlyqlToken> / <FlyqlToken type="operator">or</FlyqlToken> でつなぎ、必要に応じて <FlyqlToken type="operator">not</FlyqlToken> で否定して構成します。各条件は比較式または truthy チェックです。

## 基本的なクエリ構造

<FlyqlCode code={`status = 200 and active and not archived`} />

- <FlyqlCode code={`status = 200`} inline /> — <FlyqlToken type="key">status</FlyqlToken> フィールドが <FlyqlToken type="value">200</FlyqlToken> と等しい
- <FlyqlCode code={`active`} inline /> — <FlyqlToken type="key">active</FlyqlToken> フィールドが truthy な値を持つ
- <FlyqlCode code={`not archived`} inline /> — <FlyqlToken type="key">archived</FlyqlToken> フィールドが falsy である（null、空、0、または false）

## 追加の例

<FlyqlCode code={`service != 'api' or user = "john doe"`} />

<FlyqlCode code={`message ~ "error.*" and not debug`} />

<FlyqlCode code={`(a = 1 or b = 2) and not (c = 3 and d = 4)`} />

<FlyqlCode code={`status in [200, 201] and method not in ['DELETE', 'PUT']`} />

<FlyqlCode code={`message has 'error' and tags not has 'debug'`} />

## 一般ルール

- **単独のキー** — 演算子なしのキーは truthy チェックとして扱われます: <FlyqlCode code={`active`} inline />
- **比較式** — 演算子付きのキーには、対応する値が必要です: <FlyqlCode code={`status = 200`} inline />
- **空白** — 演算子の前後のスペースは任意です: <FlyqlCode code={`status=200`} inline /> と <FlyqlCode code={`status = 200`} inline /> は同じ意味です
- **左から右への評価** — <FlyqlToken type="operator">and</FlyqlToken> と <FlyqlToken type="operator">or</FlyqlToken> の間に優先順位はありません: <FlyqlCode code={`a = 1 or b = 2 and c = 3`} inline /> は <FlyqlCode code={`(a = 1 or b = 2) and c = 3`} inline /> として parse され、`a = 1 or (b = 2 and c = 3)` ではありません
- **括弧** — グループ化が重要な場合は必ず <FlyqlToken type="operator">( )</FlyqlToken> を使ってください: <FlyqlCode code={`a = 1 or (b = 2 and c = 3)`} inline />

## 構文セクション

- [演算子](/ja/syntax/operators/) — 比較演算子（<FlyqlToken type="operator">=</FlyqlToken>、<FlyqlToken type="operator">!=</FlyqlToken>、<FlyqlToken type="operator">{'>'}</FlyqlToken>、<FlyqlToken type="operator">{'>='}</FlyqlToken>、<FlyqlToken type="operator">{'<'}</FlyqlToken>、<FlyqlToken type="operator">{'<='}</FlyqlToken>）
- [ブールロジック](/ja/syntax/boolean-logic/) — <FlyqlToken type="operator">and</FlyqlToken>、<FlyqlToken type="operator">or</FlyqlToken>、<FlyqlToken type="operator">not</FlyqlToken>、左から右への評価、括弧によるグループ化
- [パターンマッチ](/ja/syntax/pattern-matching/) — regex（<FlyqlToken type="operator">{'~'}</FlyqlToken>、<FlyqlToken type="operator">{'!~'}</FlyqlToken>）と SQL ワイルドカード（<FlyqlToken type="operator">{'%'}</FlyqlToken>、<FlyqlToken type="operator">{'_'}</FlyqlToken>）を使う <FlyqlToken type="operator">like</FlyqlToken>/<FlyqlToken type="operator">ilike</FlyqlToken>
- [リスト](/ja/syntax/lists/) — リスト値に対する <FlyqlToken type="operator">in</FlyqlToken> と <FlyqlToken type="operator">not in</FlyqlToken>
- [包含チェック](/ja/syntax/operators/#包含チェック) — 部分文字列、キー存在、要素所属を判定する <FlyqlToken type="operator">has</FlyqlToken> と <FlyqlToken type="operator">not has</FlyqlToken>
- [値と式](/ja/syntax/values/) — truthy/falsy、クォート付き文字列、数値
- [ネストしたキー](/ja/syntax/nested-keys/) — ドット区切りパスとクォート付きキーセグメント
- [パラメータ](/ja/syntax/parameters/) — `bindParams()` で実行時に解決される `$name` と `$1` のプレースホルダ
