---
title: Python 빠른 시작
description: Python 프로젝트에서 FlyQL 설치 및 사용
---

## 설치

Python용 FlyQL은 PyPI가 아니라 git에서 설치합니다.

```bash
pip install "flyql @ git+https://github.com/iamtelescope/flyql.git#subdirectory=python"
```

이렇게 하면 `flyql` 패키지와 의존성인 `google-re2`가 함께 설치됩니다.

**요구 사항:** Python 3.10+

## 쿼리 parse하기

```python
from flyql import parse

result = parse("status = 200 and active")
print(result.root)
```

`parse()`는 AST를 나타내는 `root` 노드를 가진 `Parser` 객체를 반환합니다. 입력이 잘못되면 `ParserError`를 발생시킵니다.

## SQL 생성

Generator를 사용하면 AST를 파라미터화된 WHERE 절로 바꿀 수 있습니다. 각 데이터베이스 방언에는 전용 generator가 있습니다.

### ClickHouse

```python
from flyql import parse
from flyql.generators.clickhouse.generator import to_sql_where
from flyql.generators.clickhouse.column import Column

result = parse("status >= 400 and host = prod*")

columns = {
    "status": Column("status", False, "UInt32"),
    "host": Column("host", False, "String"),
}

sql = to_sql_where(result.root, columns)
print(sql)
```

PostgreSQL과 StarRocks generator도 같은 패턴을 따릅니다. 대신 `flyql.generators.postgresql` 또는 `flyql.generators.starrocks`에서 import하세요.

## 메모리에서 매칭하기

SQL을 생성하지 않고 데이터 레코드에 대해 쿼리를 평가할 수 있습니다.

```python
from flyql import parse
from flyql.matcher import Evaluator, Record


result = parse("status = 200 and active")

data = {
    "status": 200,
    "active": True,
    "host": "prod-api-01",
}

evaluator = Evaluator()
matches = evaluator.evaluate(result.root, Record(data))
print(f"Matches: {matches}")  # True
```

## 컬럼 표현식 parse하기

컬럼 표현식을 segments, transformers, aliases를 포함하는 구조화된 데이터로 parse할 수 있습니다.

```python
from flyql.columns import parse, parse_to_json

# 기본 컬럼 parse (transformers는 기본적으로 비활성화)
parsed = parse("message, status")
for col in parsed:
    print(f"{col.name} (display: {col.display_name!r}, segments: {col.segments})")

# capabilities로 transformers 활성화
with_transforms = parse("message|chars(25) as msg, status", capabilities={"transformers": True})

# 또는 API 응답용으로 직접 JSON으로 직렬화
json_str = parse_to_json("message, status|upper", capabilities={"transformers": True})
print(json_str)
```

## 쿼리에서 Transformers 사용하기

transformer가 포함된 쿼리를 parse하고 SQL을 생성할 수 있습니다.

```python
from flyql import parse
from flyql.generators.clickhouse.generator import to_sql_where, Column

# transformer가 포함된 쿼리 parse
result = parse("message|upper = 'ERROR'")

# ClickHouse용 SQL 생성
columns = {"message": Column("message", False, "String")}
sql = to_sql_where(result.root, columns)
print(sql)  # equals(upper(message), 'ERROR')
```

전체 문법은 [Transformers](/ko/syntax/transformers/)를 참고하세요.

## 다음 단계

- [구문 참조](/ko/syntax/) — 모든 연산자, 불리언 로직, 패턴 등
