---
title: Syntax Overview
description: Basic query structure and syntax rules for FlyQL
---

import FlyqlCode from '../../../components/FlyqlCode.astro'
import FlyqlToken from '../../../components/FlyqlToken.astro'

FlyQL queries consist of one or more conditions joined by <FlyqlToken type="operator">and</FlyqlToken> / <FlyqlToken type="operator">or</FlyqlToken>, optionally negated with <FlyqlToken type="operator">not</FlyqlToken>. Each condition is either a comparison or a truthy check.

## Basic Query Structure

<FlyqlCode code={`status = 200 and active and not archived`} />

- <FlyqlCode code={`status = 200`} inline /> — <FlyqlToken type="key">status</FlyqlToken> field equals <FlyqlToken type="value">200</FlyqlToken>
- <FlyqlCode code={`active`} inline /> — <FlyqlToken type="key">active</FlyqlToken> field has a truthy value
- <FlyqlCode code={`not archived`} inline /> — <FlyqlToken type="key">archived</FlyqlToken> field is falsy (null, empty, zero, or false)

## More Examples

<FlyqlCode code={`service != 'api' or user = "john doe"`} />

<FlyqlCode code={`message ~ "error.*" and not debug`} />

<FlyqlCode code={`(a = 1 or b = 2) and not (c = 3 and d = 4)`} />

<FlyqlCode code={`status in [200, 201] and method not in ['DELETE', 'PUT']`} />

<FlyqlCode code={`message has 'error' and tags not has 'debug'`} />

## General Rules

- **Standalone keys** — A key without an operator is treated as a [truthy check](/syntax/values/#truthyfalsy-expressions): <FlyqlCode code={`active`} inline />
- **Comparisons** — A key with an operator must have a corresponding value: <FlyqlCode code={`status = 200`} inline />
- **Whitespace** — Spaces around operators are optional: <FlyqlCode code={`status=200`} inline /> and <FlyqlCode code={`status = 200`} inline /> are equivalent
- **Standard operator precedence** — <FlyqlToken type="operator">not</FlyqlToken> binds tightest, then <FlyqlToken type="operator">and</FlyqlToken>, then <FlyqlToken type="operator">or</FlyqlToken> (same as SQL, Python, JavaScript, Go): <FlyqlCode code={`a = 1 or b = 2 and c = 3`} inline /> is parsed as <FlyqlCode code={`a = 1 or (b = 2 and c = 3)`} inline />
- **Parentheses** — Use <FlyqlToken type="operator">( )</FlyqlToken> to override the default precedence: <FlyqlCode code={`(a = 1 or b = 2) and c = 3`} inline />

## Syntax Sections

- [Operators](/syntax/operators/) — Comparison, regex, list membership, pattern matching, and containment
- [Boolean Logic](/syntax/boolean-logic/) — <FlyqlToken type="operator">and</FlyqlToken>, <FlyqlToken type="operator">or</FlyqlToken>, <FlyqlToken type="operator">not</FlyqlToken>, standard precedence, parenthesized grouping
- [Pattern Matching](/syntax/pattern-matching/) — Regex (<FlyqlToken type="operator">{'~'}</FlyqlToken>, <FlyqlToken type="operator">{'!~'}</FlyqlToken>) and <FlyqlToken type="operator">like</FlyqlToken>/<FlyqlToken type="operator">ilike</FlyqlToken> with SQL wildcards (<FlyqlToken type="operator">{'%'}</FlyqlToken>, <FlyqlToken type="operator">{'_'}</FlyqlToken>)
- [Lists](/syntax/lists/) — <FlyqlToken type="operator">in</FlyqlToken> and <FlyqlToken type="operator">not in</FlyqlToken> with list values
- [Containment](/syntax/operators/#containment-check) — <FlyqlToken type="operator">has</FlyqlToken> and <FlyqlToken type="operator">not has</FlyqlToken> for substring, key existence, and item membership
- [Values & Expressions](/syntax/values/) — Truthy/falsy, strings, numbers, booleans, null, columns, arrays, temporal functions
- [Nested Keys](/syntax/nested-keys/) — Dot-separated paths and quoted key segments
- [Parameters](/syntax/parameters/) — `$name` and `$1` placeholders resolved at runtime via `bindParams()`
- [Reserved Words](/syntax/reserved-words/) — Using keyword-named columns (<FlyqlToken type="operator">not</FlyqlToken>, <FlyqlToken type="operator">has</FlyqlToken>, <FlyqlToken type="operator">in</FlyqlToken>, etc.) and when quoting is needed
