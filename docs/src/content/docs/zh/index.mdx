---
title: 文档
description: 一种轻量级、天然防注入的查询语言，可生成多种 SQL 方言
---

import FlyqlCode from '../../../components/FlyqlCode.astro'
import FlyqlQuickExampleTable from '../../../components/FlyqlQuickExampleTable.astro'
import TechLabel from '../../../components/TechLabel.astro'
import { flyqlVersion, landingIntroQuery, landingQuickExampleQuery } from '../../../data/flyql-examples'

欢迎来到 **FlyQL** 项目文档。当前版本：**{flyqlVersion}**（[更新日志](/zh/changelog)）。

FlyQL 是一种轻量级、易读的查询语言，专门用于数据过滤。它可转换为适用于 **ClickHouse**、**PostgreSQL** 和 **StarRocks** 的防注入参数化 SQL，并提供 **Go**、**Python** 和 **JavaScript** 的完整实现。只需编写一次查询，FlyQL 即可为你的数据库生成特定方言的 SQL，或者直接在内存中匹配数据记录。

<FlyqlCode code={landingIntroQuery} />

无论你的后端使用 ClickHouse、PostgreSQL、StarRocks，还是内存中的数据集，这条查询的行为都保持一致。

## 核心特性

- **天然防注入**：语法本身无法表达子查询、JOIN、UNION 或 DDL。安全性来自语言设计，而不是额外防御。所有生成的 SQL 都会安全处理值。
- **多 SQL 方言支持**：同一套查询语法，可输出不同数据库的正确 SQL。FlyQL 能针对 ClickHouse、PostgreSQL 和 StarRocks 生成符合各自特性的语句，包括 JSON 路径、Map 访问和数组处理。
- **多语言实现**：提供 Go、Python 和 JavaScript 的完整实现。你可以在前端解析、在后端校验、在任意位置生成 SQL，同一条查询在不同环境中保持一致。
- **参数化查询**：`$name` 和 `$1` 占位符可在运行时通过 `bindParams()` 解析。一次模板化查询，稍后安全地绑定值 —— 无需字符串拼接，无需手动转义。
- **依赖最小化**：Go 与 JavaScript 实现均使用手写状态机解析器，不依赖外部库。Python 需要 `google-re2` 以进行基于 RE2 的安全正则匹配。集成简单，不容易引入额外冲突。

## 快速示例

<FlyqlCode code={landingQuickExampleQuery} />

<FlyqlQuickExampleTable
  componentHeader="组成部分"
  meaningHeader="含义"
  meanings={{
    service: '`service` 字段不等于 `"api"`',
    status: '`status` 大于或等于 `400`',
    level: '`level` 字段等于 `"error"`',
    message: '`message` 匹配正则表达式',
    boolean: '支持优先级的布尔逻辑',
    grouping: '显式分组',
  }}
/>

## 支持的语言

| 语言 | 解析器 | 内存匹配 |
|------|--------|----------|
| <TechLabel icon="/icons/go.svg" label="Go" variant="go" /> | 支持 | 支持 |
| <TechLabel icon="/icons/python.svg" label="Python" /> | 支持 | 支持 |
| <TechLabel icon="/icons/javascript.svg" label="JavaScript" /> | 支持 | 支持 |

## SQL 生成器

| 数据库 | Go | Python | JavaScript |
|--------|----|--------|------------|
| <TechLabel icon="/icons/clickhouse_light.svg" label="ClickHouse" variant="db" /> | 支持 | 支持 | 支持 |
| <TechLabel icon="/icons/postgresql.svg" label="PostgreSQL" variant="db" /> | 支持 | 支持 | 支持 |
| <TechLabel icon="/icons/starrocks.svg" label="StarRocks" variant="db" /> | 支持 | 支持 | 支持 |
