---
title: 模式匹配
description: FlyQL 中的正则与通配匹配
---

## 正则匹配

使用 `~` 将值与正则表达式匹配，使用 `!~` 表示不匹配：

```
message~"error.*timeout"
path!~"^/health"
```

| 操作符 | 含义 |
|--------|------|
| `~` | 值匹配正则 |
| `!~` | 值不匹配正则 |

### 正则语法

正则语法依赖具体后端：

- **ClickHouse**：使用 `match()`，采用 RE2 语法
- **PostgreSQL**：使用 `~` 和 `!~`，采用 POSIX 正则
- **StarRocks**：使用 `regexp` 函数
- **内存匹配器**：使用宿主语言的正则引擎（Go `regexp`、Python `re2`、JS `RegExp`）

当正则中包含空格或特殊字符时，应使用引号：

```
message~"connection (refused|reset)"
host~"^prod-[0-9]+"
```

### 忽略大小写

如果要进行大小写不敏感匹配，可在模式开头加上 `(?i)`：

```
message~"(?i)error"
level~"(?i)^(warn|error|fatal)$"
```

支持情况如下：

| 后端 | 是否支持 `(?i)` | 说明 |
|------|-----------------|------|
| ClickHouse | 支持 | RE2 |
| PostgreSQL | 支持 | POSIX regex |
| StarRocks | 支持 | RE2 |
| Go 内存匹配器 | 支持 | Go `regexp` |
| Python 内存匹配器 | 支持 | `google-re2` |
| JavaScript 内存匹配器 | 不支持 | ECMAScript `RegExp` 不支持行内 `(?i)` |

JavaScript 的内存匹配场景下，可以使用字符类替代：

```
message~"[eE][rR][rR][oO][rR]"
```

## 通配匹配

对于简单的前缀匹配，可以在未加引号的值末尾使用 `*`：

```
host=prod*
service=api-*
```

这表示匹配所有以指定前缀开头的值。通配符只支持出现在末尾，像 `*prod` 或 `pr*od` 这样的写法不受支持。

### 通配符与正则的区别

| 模式 | 语法 | 示例 |
|------|------|------|
| 通配前缀 | `key=value*` | `host=prod*` |
| 完整正则 | `key~"pattern"` | `host~"^prod-[0-9]+"` |

简单前缀匹配用通配符，更复杂的模式请使用正则。

## 示例

```
# 匹配包含 "error" 或 "fail" 的消息
message~"error|fail"

# 匹配以 /api/v2 开头的路径
path~"^/api/v2"

# 排除健康检查路径
path!~"^/health"

# 主机名前缀匹配
host=prod*

# 组合示例
host=prod* and message~"timeout.*" and path!~"^/health"
```

