---
title: PostgreSQL
description: Ejemplos de SQL generado para PostgreSQL
---

El SQL de PostgreSQL usa identificadores entre comillas dobles, `~` para regex, `AND` y `OR` en mayúsculas y operadores JSONB para acceso anidado.

## Ejemplos básicos

| FlyQL | SQL generado |
|-------|--------------|
| `message='hello'` | `"message" = 'hello'` |
| `message!='hello'` | `"message" != 'hello'` |
| `count>10` | `"count" > 10` |
| `count<=100` | `"count" <= 100` |
| `message~'error.*'` | `"message" ~ 'error.*'` |
| `message!~'test.*'` | `"message" !~ 'test.*'` |
| `host=prod*` | `"host" LIKE 'prod%'` |

## Operadores booleanos

PostgreSQL usa `AND` y `OR` en mayúsculas, a diferencia de ClickHouse y StarRocks:

| FlyQL | SQL generado |
|-------|--------------|
| `a='1' and b>10` | `("a" = '1' AND "b" > 10)` |
| `a='1' or b='2'` | `("a" = '1' OR "b" = '2')` |
| `not a='1'` | `NOT ("a" = '1')` |

## Pertenencia a listas

| FlyQL | SQL generado |
|-------|--------------|
| `status in [200, 201]` | `"status" IN (200, 201)` |
| `env not in ['dev', 'test']` | `"env" NOT IN ('dev', 'test')` |
| `status in []` | `FALSE` |
| `status not in []` | `TRUE` |

## Contención (`has` / `not has`)

El operador `has` genera SQL distinto según el tipo de columna:

| Tipo de columna | FlyQL | SQL generado |
|-----------------|-------|--------------|
| String | `message has 'error'` | `position('error' in "message") > 0` |
| String con negación | `message not has 'error'` | `("message" IS NULL OR position('error' in "message") = 0)` |
| Array | `tags has 'web'` | `'web' = ANY("tags")` |
| Array con negación | `tags not has 'web'` | `NOT ('web' = ANY("tags"))` |
| JSONB | `data has 'key'` | `"data" ? 'key'` |
| JSONB con negación | `data not has 'key'` | `NOT ("data" ? 'key')` |
| Hstore | `metadata has 'key'` | `"metadata" ? 'key'` |

## Columnas JSONB

PostgreSQL usa operadores JSONB con comprobación de tipo mediante `jsonb_typeof()`:

### Comparación de strings

| FlyQL | SQL generado |
|-------|--------------|
| `data.name='test'` | `(jsonb_typeof("data"->'name') = 'string' AND "data"->>'name' = 'test')` |
| `data.user.name='john'` | `(jsonb_typeof("data"->'user'->'name') = 'string' AND "data"->'user'->>'name' = 'john')` |

### Comparación numérica

Las comparaciones numéricas convierten el valor extraído:

| FlyQL | SQL generado |
|-------|--------------|
| `data.age>25` | `(jsonb_typeof("data"->'age') = 'number' AND ("data"->>'age')::numeric > 25)` |

### Operadores JSONB

- `->` devuelve JSONB y se usa para el recorrido intermedio de la ruta
- `->>` devuelve texto y se usa para extraer el valor final
- `jsonb_typeof()` comprueba el tipo JSON antes de comparar

## Columnas Hstore (Map)

PostgreSQL usa el tipo hstore con el operador `->` para acceder a claves:

| FlyQL | SQL generado |
|-------|--------------|
| `metadata.key1='value1'` | `"metadata"->'key1' = 'value1'` |
| `metadata.key1!='value1'` | `"metadata"->'key1' != 'value1'` |
| `metadata.pattern~'test.*'` | `"metadata"->'pattern' ~ 'test.*'` |

## Columnas Array

Los arrays de PostgreSQL son de base 1, así que FlyQL convierte automáticamente el índice base 0:

| FlyQL | SQL generado |
|-------|--------------|
| `tags.0='first'` | `"tags"[1] = 'first'` |
| `tags.1='second'` | `"tags"[2] = 'second'` |
| `tags.0~'tag.*'` | `"tags"[1] ~ 'tag.*'` |

### Configuración de columnas

Las columnas de PostgreSQL no tienen parámetro `jsonString`; el tipo JSONB se detecta a partir del tipo de columna:

```go
// Go
columns := map[string]*postgresql.Column{
    "message": postgresql.NewColumn("message", "varchar", nil),
    "data":    postgresql.NewColumn("data", "jsonb", nil),
}
```

```javascript
// JavaScript
const columns = {
    message: newColumn("message", "varchar", null),
    data:    newColumn("data", "jsonb", null),
}
```

## Transformers

Los transformers generan llamadas estándar de PostgreSQL:

| FlyQL | SQL generado |
|-------|--------------|
| `status\|upper = "ERROR"` | `UPPER(status) = 'ERROR'` |
| `status\|lower = "info"` | `LOWER(status) = 'info'` |
| `message\|len > 100` | `LENGTH(message) > 100` |
| `message\|lower\|len > 100` | `LENGTH(LOWER(message)) > 100` |
| `tags\|split(",") has "urgent"` | `'urgent' = ANY(STRING_TO_ARRAY(tags, ','))` |
