---
title: Lógica booleana
description: Operadores booleanos, precedencia estándar (NOT > AND > OR) y agrupación con paréntesis en FlyQL
---

import FlyqlCode from '../../../../components/FlyqlCode.astro'
import FlyqlToken from '../../../../components/FlyqlToken.astro'

## Operadores booleanos

FlyQL usa tres operadores booleanos para combinar condiciones:

| Operador | Significado | Ejemplo |
|----------|-------------|---------|
| <FlyqlToken type="operator">and</FlyqlToken> | Ambas condiciones deben ser true | <FlyqlCode code={`status = 200 and active`} inline /> |
| <FlyqlToken type="operator">or</FlyqlToken> | Cualquiera de las dos condiciones puede ser true | <FlyqlCode code={`level = 'error' or level = 'critical'`} inline /> |
| <FlyqlToken type="operator">not</FlyqlToken> | Niega la expresión o grupo inmediatamente siguiente | <FlyqlCode code={`not archived`} inline /> |

### Negación combinada

<FlyqlToken type="operator">not</FlyqlToken> puede combinarse con <FlyqlToken type="operator">and</FlyqlToken> y <FlyqlToken type="operator">or</FlyqlToken>:

<FlyqlCode code={`active and not archived`} />
<FlyqlCode code={`level = 'error' or not healthy`} />

## Precedencia de operadores

FlyQL sigue la **precedencia de operadores estándar** — las mismas reglas que SQL, Python, JavaScript, Go y cualquier lenguaje de consulta convencional:

| Precedencia | Operador | Alcance |
|------------:|----------|---------|
| 1 (más fuerte) | <FlyqlToken type="operator">not</FlyqlToken> | Se enlaza con su operando inmediato — la expresión o el grupo entre paréntesis que le sigue |
| 2 | <FlyqlToken type="operator">and</FlyqlToken> | Más fuerte que <FlyqlToken type="operator">or</FlyqlToken> |
| 3 (más débil) | <FlyqlToken type="operator">or</FlyqlToken> | El más débil — combina cadenas completas de AND |

En concreto:

<FlyqlCode code={`a = 1 or b = 2 and c = 3`} />

se parsea como:

<FlyqlCode code={`a = 1 or (b = 2 and c = 3)`} />

porque <FlyqlToken type="operator">and</FlyqlToken> se enlaza con más fuerza que <FlyqlToken type="operator">or</FlyqlToken>. Las cadenas con la misma precedencia son asociativas por la izquierda, por lo que <FlyqlCode code={`a = 1 and b = 2 and c = 3`} inline /> se parsea como <FlyqlCode code={`(a = 1 and b = 2) and c = 3`} inline />.

<FlyqlToken type="operator">not</FlyqlToken> es el que se enlaza con mayor fuerza y se aplica únicamente a la expresión o al grupo que le sigue — nunca a toda la expresión anterior.

## Agrupación con paréntesis

Usa paréntesis para anular la precedencia por defecto cuando quieras que <FlyqlToken type="operator">or</FlyqlToken> se evalúe antes que <FlyqlToken type="operator">and</FlyqlToken>:

<FlyqlCode code={`(status >= 400 or level = 'error') and service = 'api'`} />

Sin los paréntesis, el mismo texto fuente

<FlyqlCode code={`status >= 400 or level = 'error' and service = 'api'`} />

se parsea como

<FlyqlCode code={`status >= 400 or (level = 'error' and service = 'api')`} />

lo cual es una consulta diferente. Cuando quieras que <FlyqlToken type="operator">or</FlyqlToken> domine, recurre explícitamente a los paréntesis.

### Agrupación anidada

Los paréntesis pueden anidarse:

<FlyqlCode code={`(a = 1 or b = 2) and not (c = 3 and d = 4)`} />

### Negar grupos

<FlyqlToken type="operator">not</FlyqlToken> se aplica a todo el grupo:

<FlyqlCode code={`not (status = 200 and healthy)`} />

Esto niega la condición combinada — solo es true cuando <FlyqlToken type="key">status</FlyqlToken> no es 200 o <FlyqlToken type="key">healthy</FlyqlToken> es false.

## Negación

<FlyqlToken type="operator">not</FlyqlToken> niega cualquier expresión — una comprobación truthy, una comparación o un grupo:

<FlyqlCode code={`not active`} />
<FlyqlCode code={`not status = 200`} />
<FlyqlCode code={`not (a = 1 and b = 2)`} />
<FlyqlCode code={`active and not archived`} />

La doble negación se cancela: <FlyqlCode code={`not not active`} inline /> es equivalente a <FlyqlCode code={`active`} inline />.

## Ejemplos

<FlyqlCode code={`status = 200 and service = 'api'`} />
<FlyqlCode code={`level = 'error' or level = 'critical'`} />
<FlyqlCode code={`status >= 400 or level = 'error' and service = 'api'`} />
<FlyqlCode code={`(status >= 400 or level = 'error') and service = 'api'`} />
<FlyqlCode code={`(status >= 500 and host = 'prod') or (level = 'error' and not service = 'healthcheck')`} />
