---
title: Werte und Typen
description: Werttypen, typisierte Literale und Ausdrücke in FlyQL
---

import FlyqlCode from '../../../../components/FlyqlCode.astro'
import FlyqlToken from '../../../../components/FlyqlToken.astro'

FlyQL unterstützt 10 Werttypen: **Integer**, **BigInt**, **Float**, **String**, **Boolean**, **Null**, **Array**, **Column**, **Function** und **Parameter**. Jeder Wert im AST trägt ein explizites Feld `value_type`.

## Werttypen

### Integer

Ganzzahlen innerhalb des int64-Bereichs:

<FlyqlCode code={`count = 42`} />
<FlyqlCode code={`status != 200`} />
<FlyqlCode code={`age >= 18`} />

AST: `value_type: "integer"`

### BigInt

Ganzzahlen, die den int64-Bereich (Go/Python) oder `MAX_SAFE_INTEGER` (JavaScript) überschreiten:

<FlyqlCode code={`id = 18446744073709551615`} />

AST: `value_type: "bigint"`

### Float

Zahlen mit Dezimalpunkt:

<FlyqlCode code={`price > 9.99`} />
<FlyqlCode code={`ratio = 3.14`} />
<FlyqlCode code={`temperature<=-0.5`} />

AST: `value_type: "float"`

### String

Textwerte **müssen in Anführungszeichen** gesetzt werden, entweder einfach oder doppelt. Werte ohne Anführungszeichen werden als Spaltenreferenzen und nicht als Strings interpretiert.

<FlyqlCode code={`level = 'error'`} />
<FlyqlCode code={`user = "John Doe"`} />
<FlyqlCode code={`message = 'hello world'`} />
<FlyqlCode code={`count = '42'`} />

AST: `value_type: "string"`

Anführungszeichen lassen sich mit Backslash escapen:

```
user = 'John\'s'
message="said \"hello\""
```

### Boolean

Die Schlüsselwörter <FlyqlToken type="boolean">true</FlyqlToken> und <FlyqlToken type="boolean">false</FlyqlToken> (kleingeschrieben, case-sensitive):

<FlyqlCode code={`active = true`} />
<FlyqlCode code={`debug = false`} />
<FlyqlCode code={`active != true`} />

AST: `value_type: "boolean"`, der Wert ist ein nativer Boolean.

Großgeschriebene Varianten (`True`, `FALSE`) werden als Spaltenreferenzen behandelt, nicht als Booleans.

**SQL-Generierung:**
| Dialekt | FlyQL | Generiertes SQL |
|---------|-------|---------------|
| ClickHouse | <FlyqlCode code={`field = true`} inline /> | `field = true` |
| ClickHouse | <FlyqlCode code={`field = false`} inline /> | `field = false` |
| PostgreSQL | <FlyqlCode code={`field = true`} inline /> | `field = TRUE` |
| PostgreSQL | <FlyqlCode code={`field = false`} inline /> | `field = FALSE` |
| StarRocks | <FlyqlCode code={`field = true`} inline /> | `field = true` |
| StarRocks | <FlyqlCode code={`field = false`} inline /> | `field = false` |

### Null

Das Schlüsselwort <FlyqlToken type="null">null</FlyqlToken> (kleingeschrieben, case-sensitive):

<FlyqlCode code={`field = null`} />
<FlyqlCode code={`field != null`} />

AST: `value_type: "null"`, der Wert ist natives null.

Großgeschriebene Varianten (`Null`, `NULL`) werden als Spaltenreferenzen behandelt, nicht als null.

**Einschränkungen:** Mit null sind nur die Operatoren <FlyqlToken type="operator">=</FlyqlToken> und <FlyqlToken type="operator">!=</FlyqlToken> gültig. Die Verwendung von <FlyqlToken type="operator">{'>'}</FlyqlToken>, <FlyqlToken type="operator">{'<'}</FlyqlToken>, <FlyqlToken type="operator">{'>='}</FlyqlToken>, <FlyqlToken type="operator">{'<='}</FlyqlToken>, <FlyqlToken type="operator">{'~'}</FlyqlToken> oder <FlyqlToken type="operator">{'!~'}</FlyqlToken> mit null erzeugt einen Parse-Fehler.

**SQL-Generierung:** <FlyqlCode code={`field = null`} inline /> erzeugt `field IS NULL`, <FlyqlCode code={`field != null`} inline /> erzeugt `field IS NOT NULL` (in allen Dialekten).

### Spaltenreferenz

Jeder Wert ohne Anführungszeichen, der keine Zahl, kein Boolean, kein null und keine temporale Funktion ist, wird als **Spaltenreferenz** interpretiert – ein Verweis auf eine andere Spalte für Spalte-zu-Spalte-Vergleiche:

<FlyqlCode code={`price > min_price`} />
<FlyqlCode code={`updated_at > created_at`} />
<FlyqlCode code={`field = otherfield`} />

AST: `value_type: "column"`

Wenn du einen Stringwert vergleichen willst, **musst** du Anführungszeichen verwenden:

<FlyqlCode code={`level = 'error'`} />

Ohne Anführungszeichen behandelt <FlyqlCode code={`level = error`} inline /> `error` als Verweis auf eine Spalte mit dem Namen `error`.

### Array

Arrays werden mit den Operatoren <FlyqlToken type="operator">in</FlyqlToken> und <FlyqlToken type="operator">not in</FlyqlToken> verwendet. Elemente dürfen gemischte Typen haben (heterogen):

<FlyqlCode code={`status in [200, 201, 202]`} />
<FlyqlCode code={`name in ['alice', 'bob']`} />
<FlyqlCode code={`field in [1, "hello", true, null]`} />

Jedes Element trägt seinen eigenen Typ im Array `values_types`.

## Truthy/Falsy-Ausdrücke

Ein einzelner Schlüssel ohne Operator prüft, ob das Feld einen truthy Wert hat:

<FlyqlCode code={`active`} />
<FlyqlCode code={`message and status`} />

Ein Wert gilt als **falsy**, wenn er:
- `null` / `None` / nicht vorhanden ist
- eine leere Zeichenkette `""` ist
- Null `0` ist
- der Boolean <FlyqlToken type="boolean">false</FlyqlToken> ist

Alles andere ist **truthy**.

Verwende <FlyqlToken type="operator">not</FlyqlToken>, um auf falsy Werte zu prüfen:

<FlyqlCode code={`not archived`} />
<FlyqlCode code={`active and not debug`} />

Truthy/Falsy-Prüfungen unterscheiden sich von Null-Vergleichen: <FlyqlCode code={`not field`} inline /> trifft zu, wenn das Feld null, leer oder Null ist. <FlyqlCode code={`field = null`} inline /> trifft nur dann zu, wenn das Feld explizit null ist.

## Leerzeichen

FlyQL ist tolerant gegenüber Leerzeichen. Leerzeichen um Operatoren sind optional:

<FlyqlCode code={`status = 200`} />
<FlyqlCode code={`status =200`} />

Alle Varianten sind äquivalent.

## LiteralKind-Konstanten

Geparste Ausdruckswerte tragen ein `LiteralKind`-Tag. Die Konstanten werden in jeder Implementierung vom Wurzelpaket exportiert.

**Python:**
```python
from flyql import LiteralKind
# LiteralKind.INTEGER, LiteralKind.BIGINT, LiteralKind.FLOAT,
# LiteralKind.STRING, LiteralKind.BOOLEAN, LiteralKind.NULL, LiteralKind.ARRAY
```

**Go:**
```go
import "github.com/iamtelescope/flyql/golang/literal"
// literal.Integer, literal.BigInt, literal.Float,
// literal.String, literal.Boolean, literal.Null, literal.Array
```

**JavaScript:**
```javascript
import { LiteralKind } from 'flyql'
// LiteralKind.INTEGER, LiteralKind.BIGINT, LiteralKind.FLOAT,
// LiteralKind.STRING, LiteralKind.BOOLEAN, LiteralKind.NULL, LiteralKind.ARRAY
```

## Temporale Funktionen

FlyQL unterstützt temporale Funktionsaufrufe als Werte auf der rechten Seite von Vergleichsausdrücken. Diese ermöglichen zeitrelative Filter, ohne Zeitstempel fest codieren zu müssen.

AST: `value_type: "function"`, `value` ist ein `FunctionCall`-Objekt.

### `ago(duration)`

Gibt einen Zeitstempel relativ zur aktuellen Zeit zurück. Unterstützt zusammengesetzte Dauern.

<FlyqlCode code={`timestamp > ago(1h)`} />
<FlyqlCode code={`created_at >= ago(7d)`} />
<FlyqlCode code={`updated_at > ago(1h30m)`} />

**Dauereinheiten:** `s` (Sekunden), `m` (Minuten), `h` (Stunden), `d` (Tage), `w` (Wochen)

### `now()`

Gibt den aktuellen Zeitstempel zurück.

<FlyqlCode code={`expires_at < now()`} />

### `today()`

Gibt das heutige Datum zurück. Akzeptiert ein optionales IANA-Zeitzonenargument.

<FlyqlCode code={`date = today()`} />
<FlyqlCode code={`date = today('Europe/Berlin')`} />

### `startOf(unit)`

Gibt den Beginn des aktuellen Tages, der Woche (Montag) oder des Monats zurück. Akzeptiert eine optionale Zeitzone.

<FlyqlCode code={`created_at > startOf('day')`} />
<FlyqlCode code={`created_at > startOf('week')`} />
<FlyqlCode code={`created_at > startOf('month', 'Asia/Tokyo')`} />

**Gültige Einheiten:** `'day'`, `'week'`, `'month'`

### Operatoreinschränkungen

Temporale Funktionen können nur mit Vergleichsoperatoren verwendet werden: `=`, `!=`, `>`, `>=`, `<`, `<=`.

Die Verwendung von `~` oder `!~` mit einer temporalen Funktion erzeugt einen Parse-Fehler. Operatoren wie `in`, `has`, `like` und `ilike` verwenden separate Parsing-Pfade und sind nicht mit Funktionswerten kompatibel.

### Spaltenreferenz ohne Klammern

Nackte Funktionsnamen ohne Klammern werden als Spaltenreferenzen behandelt, nicht als Funktionsaufrufe:

<FlyqlCode code={`field = ago`} />

Dies erzeugt `value_type: "column"`, keinen Funktionsaufruf.

## Parameter

Parameter-Platzhalter (`$name` für benannt, `$1` für positional) stehen für Werte, die zur Laufzeit von `bindParams()` aufgelöst werden. Sie können überall dort auftreten, wo ein Literalwert gültig ist – auch in IN-Listen, Argumenten temporaler Funktionen und Transformer-Argumenten.

<FlyqlCode code={`status = $code`} />
<FlyqlCode code={`level in [$primary, 'warn']`} />
<FlyqlCode code={`created > ago($duration)`} />

AST: `value_type: "parameter"`, der Wert ist ein `Parameter`-Objekt mit den Feldern `name` und `positional`.

Siehe [Parameter](/de/syntax/parameters/) für die vollständige Referenz.

