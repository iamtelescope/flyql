---
title: PostgreSQL
description: Beispiele für generiertes SQL in PostgreSQL
---

import FlyqlToken from '../../../../components/FlyqlToken.astro'
import SqlExamplesTable from '../../../../components/SqlExamplesTable.astro'
import { rows as basicExamples } from '../../../../snippets/sql/postgresql/basic-examples.js'
import { rows as booleanOperators } from '../../../../snippets/sql/postgresql/boolean-operators.js'
import { rows as listMembership } from '../../../../snippets/sql/postgresql/list-membership.js'
import { rows as containment } from '../../../../snippets/sql/postgresql/containment.js'
import { rows as jsonbString } from '../../../../snippets/sql/postgresql/jsonb-string.js'
import { rows as jsonbNumeric } from '../../../../snippets/sql/postgresql/jsonb-numeric.js'
import { rows as hstoreColumns } from '../../../../snippets/sql/postgresql/hstore-columns.js'
import { rows as arrayColumns } from '../../../../snippets/sql/postgresql/array-columns.js'
import { rows as transformers } from '../../../../snippets/sql/postgresql/transformers.js'

PostgreSQL-SQL verwendet doppelt quotierte Bezeichner, <FlyqlToken type="operator">{'~'}</FlyqlToken> für Regex, Großbuchstaben bei <FlyqlToken type="operator">and</FlyqlToken>/<FlyqlToken type="operator">or</FlyqlToken> und JSONB-Operatoren für verschachtelten Zugriff.

## Grundlegende Beispiele

<SqlExamplesTable rows={basicExamples} />

## Boolesche Operatoren

PostgreSQL verwendet Großbuchstaben bei <FlyqlToken type="operator">and</FlyqlToken>/<FlyqlToken type="operator">or</FlyqlToken> (anders als ClickHouse und StarRocks):

<SqlExamplesTable rows={booleanOperators} />

## Listenzugehörigkeit

<SqlExamplesTable rows={listMembership} />

## Enthalten (`has` / `not has`)

Der Operator <FlyqlToken type="operator">has</FlyqlToken> erzeugt je nach Spaltentyp unterschiedliches SQL:

<SqlExamplesTable
    rows={containment}
    metaHeader="Spaltentyp"
    metaLabels={["String", "String (negiert)", "Array", "Array (negiert)", "JSONB", "JSONB (negiert)", "Hstore"]}
/>

## JSONB-Spalten

PostgreSQL verwendet JSONB-Operatoren mit Typprüfung über `jsonb_typeof()`:

### String-Vergleich

<SqlExamplesTable rows={jsonbString} />

### Numerischer Vergleich

Numerische Vergleiche casten den extrahierten Wert:

<SqlExamplesTable rows={jsonbNumeric} />

### JSONB-Operatoren

- `->` gibt JSONB zurück (für die Traversierung der Zwischenpfade)
- `->>` gibt Text zurück (für die Extraktion des finalen Werts)
- `jsonb_typeof()` prüft den JSON-Typ vor dem Vergleich

## Hstore- (Map-)Spalten

PostgreSQL verwendet beim hstore-Typ den Operator `->` für den Schlüsselzugriff:

<SqlExamplesTable rows={hstoreColumns} />

## Array-Spalten

PostgreSQL-Arrays sind 1-basiert – FlyQL konvertiert den 0-basierten Index automatisch:

<SqlExamplesTable rows={arrayColumns} />

### Spaltenkonfiguration

PostgreSQL-Spalten haben keinen `jsonString`-Parameter – der JSONB-Typ wird aus dem Spaltentyp erkannt:

```go
// Go
columns := map[string]*postgresql.Column{
    "message": postgresql.NewColumn(postgresql.ColumnDef{Name: "message", Type: "varchar"}),
    "data":    postgresql.NewColumn(postgresql.ColumnDef{Name: "data", Type: "jsonb"}),
}
```

```javascript
// JavaScript
const columns = {
    message: newColumn({ name: "message", type: "varchar" }),
    data:    newColumn({ name: "data", type: "jsonb" }),
}
```

## Transformers

Transformers erzeugen PostgreSQL-Standardfunktionen:

<SqlExamplesTable rows={transformers} />
