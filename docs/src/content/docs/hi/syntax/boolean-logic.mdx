---
title: बूलियन लॉजिक
description: FlyQL में बूलियन ऑपरेटर, मानक प्राथमिकता (NOT > AND > OR), और कोष्ठक समूहीकरण
---

import FlyqlCode from '../../../../components/FlyqlCode.astro'
import FlyqlToken from '../../../../components/FlyqlToken.astro'

## बूलियन ऑपरेटर

FlyQL शर्तों को जोड़ने के लिए तीन बूलियन ऑपरेटर का उपयोग करता है:

| ऑपरेटर | अर्थ | उदाहरण |
|----------|---------|---------|
| <FlyqlToken type="operator">and</FlyqlToken> | दोनों शर्तें सत्य होनी चाहिए | <FlyqlCode code={`status = 200 and active`} inline /> |
| <FlyqlToken type="operator">or</FlyqlToken> | कोई भी एक शर्त सत्य हो सकती है | <FlyqlCode code={`level = 'error' or level = 'critical'`} inline /> |
| <FlyqlToken type="operator">not</FlyqlToken> | तुरंत बाद वाली अभिव्यक्ति या समूह को नकारता है | <FlyqlCode code={`not archived`} inline /> |

### संयुक्त निषेध

<FlyqlToken type="operator">not</FlyqlToken> को <FlyqlToken type="operator">and</FlyqlToken> और <FlyqlToken type="operator">or</FlyqlToken> के साथ जोड़ा जा सकता है:

<FlyqlCode code={`active and not archived`} />
<FlyqlCode code={`level = 'error' or not healthy`} />

## ऑपरेटर प्राथमिकता

FlyQL **मानक ऑपरेटर प्राथमिकता** का पालन करता है — वही नियम जो SQL, Python, JavaScript, Go, और हर मुख्यधारा की क्वेरी भाषा में हैं:

| प्राथमिकता | ऑपरेटर | बंधन |
|-----------:|----------|-------|
| 1 (सबसे कसा) | <FlyqlToken type="operator">not</FlyqlToken> | अपने तात्कालिक ऑपरेंड से — यानी तुरंत बाद वाली अभिव्यक्ति या कोष्ठक समूह से |
| 2 | <FlyqlToken type="operator">and</FlyqlToken> | <FlyqlToken type="operator">or</FlyqlToken> से कसकर |
| 3 (सबसे ढीला) | <FlyqlToken type="operator">or</FlyqlToken> | सबसे ढीला — पूरे AND-श्रृंखलाओं को जोड़ता है |

ठोस रूप से:

<FlyqlCode code={`a = 1 or b = 2 and c = 3`} />

को इस प्रकार पार्स किया जाता है:

<FlyqlCode code={`a = 1 or (b = 2 and c = 3)`} />

क्योंकि <FlyqlToken type="operator">and</FlyqlToken>, <FlyqlToken type="operator">or</FlyqlToken> से अधिक कसकर बंधता है। समान-प्राथमिकता वाली श्रृंखलाएँ बाएँ-साहचर्य होती हैं, इसलिए <FlyqlCode code={`a = 1 and b = 2 and c = 3`} inline /> को <FlyqlCode code={`(a = 1 and b = 2) and c = 3`} inline /> के रूप में पार्स किया जाता है।

<FlyqlToken type="operator">not</FlyqlToken> सबसे कसकर बंधता है और केवल उस अभिव्यक्ति या समूह पर लागू होता है जो उसके तुरंत बाद आता है — कभी भी पूरी पूर्ववर्ती अभिव्यक्ति पर नहीं।

## कोष्ठक समूहीकरण

जब आप चाहते हैं कि <FlyqlToken type="operator">or</FlyqlToken> का मूल्यांकन <FlyqlToken type="operator">and</FlyqlToken> से पहले हो, तो डिफ़ॉल्ट प्राथमिकता को ओवरराइड करने के लिए कोष्ठक का उपयोग करें:

<FlyqlCode code={`(status >= 400 or level = 'error') and service = 'api'`} />

कोष्ठक के बिना, वही स्रोत

<FlyqlCode code={`status >= 400 or level = 'error' and service = 'api'`} />

इस प्रकार पार्स होता है

<FlyqlCode code={`status >= 400 or (level = 'error' and service = 'api')`} />

जो एक बिल्कुल अलग क्वेरी है। जब आप चाहते हैं कि <FlyqlToken type="operator">or</FlyqlToken> प्रभावी हो, तो स्पष्ट रूप से कोष्ठक का सहारा लें।

### नेस्टेड समूहीकरण

कोष्ठक को नेस्ट किया जा सकता है:

<FlyqlCode code={`(a = 1 or b = 2) and not (c = 3 and d = 4)`} />

### समूहों का निषेध

<FlyqlToken type="operator">not</FlyqlToken> पूरे समूह पर लागू होता है:

<FlyqlCode code={`not (status = 200 and healthy)`} />

यह संयुक्त शर्त को नकारता है — केवल तभी सत्य जब <FlyqlToken type="key">status</FlyqlToken> 200 नहीं है या <FlyqlToken type="key">healthy</FlyqlToken> false है।

## निषेध

<FlyqlToken type="operator">not</FlyqlToken> किसी भी अभिव्यक्ति को नकारता है — truthy जाँच, तुलना, या समूह:

<FlyqlCode code={`not active`} />
<FlyqlCode code={`not status = 200`} />
<FlyqlCode code={`not (a = 1 and b = 2)`} />
<FlyqlCode code={`active and not archived`} />

दोहरा निषेध रद्द हो जाता है: <FlyqlCode code={`not not active`} inline /> <FlyqlCode code={`active`} inline /> के समतुल्य है।

## उदाहरण

<FlyqlCode code={`status = 200 and service = 'api'`} />
<FlyqlCode code={`level = 'error' or level = 'critical'`} />
<FlyqlCode code={`status >= 400 or level = 'error' and service = 'api'`} />
<FlyqlCode code={`(status >= 400 or level = 'error') and service = 'api'`} />
<FlyqlCode code={`(status >= 500 and host = 'prod') or (level = 'error' and not service = 'healthcheck')`} />
