---
title: Dokumentation
description: Eine leichtgewichtige, injektionssichere Abfragesprache zur Generierung mehrerer SQL-Dialekte
---

import FlyqlCode from '../../../components/FlyqlCode.astro'
import FlyqlQuickExampleTable from '../../../components/FlyqlQuickExampleTable.astro'
import TechLabel from '../../../components/TechLabel.astro'
import { flyqlVersion, landingIntroQuery, landingQuickExampleQuery } from '../../../data/flyql-examples'

Willkommen zur **FlyQL**-Projektdokumentation. Aktuelle Version: **{flyqlVersion}** ([Änderungsprotokoll](/de/changelog)).

FlyQL ist eine leichtgewichtige, gut lesbare Abfragesprache zum Filtern von Daten. Sie transpiliert in injektionssicheres, parametrisiertes SQL für **ClickHouse**, **PostgreSQL** und **StarRocks** — mit vollständigen Implementierungen in **Go**, **Python** und **JavaScript**. Schreibe eine Abfrage einmal, und FlyQL generiert dialektspezifisches SQL für deine Datenbank — oder wertet sie im Speicher gegen Datensätze aus.

<FlyqlCode code={landingIntroQuery} />

Diese Abfrage funktioniert identisch, egal ob dein Backend ClickHouse, PostgreSQL, StarRocks oder einen Datensatz im Speicher verwendet.

## Hauptmerkmale

- **Injektionssicher per Design**: Die Grammatik kann keine Subqueries, `JOIN`s, `UNION`s oder DDL ausdrücken. Sicherheit ist strukturell verankert, nicht nur defensiv umgesetzt. Das gesamte generierte SQL verwendet parametrisierte Werte.
- **Mehrere SQL-Dialekte**: Eine Abfragesprache, dialektspezifische Ausgabe. FlyQL generiert korrektes SQL für ClickHouse, PostgreSQL und StarRocks mit passender Typbehandlung für JSON-Pfade, Map-Zugriffe und Arrays.
- **Mehrere Programmiersprachen**: Vollständige Implementierungen in Go, Python und JavaScript. Parse im Frontend, validiere im Backend, generiere SQL überall, gleiche Abfrage, gleiches Ergebnis.
- **Parametrisierte Abfragen**: `$name`- und `$1`-Platzhalter, die zur Laufzeit über `bindParams()` aufgelöst werden. Templatisiere eine Abfrage einmal und binde Werte später sicher – keine Stringverkettung, kein manuelles Escapen.
- **Keine Abhängigkeiten**: Ein handgeschriebener State-Machine-Parser ohne externe Abhängigkeiten in jeder Sprache. Leicht integrierbar und konfliktfrei.

## Schnelles Beispiel

<FlyqlCode code={landingQuickExampleQuery} />

<FlyqlQuickExampleTable
  componentHeader="Komponente"
  meaningHeader="Bedeutung"
  meanings={{
    service: 'Feld `service` ist nicht `"api"`',
    status: '`status` ist 400 oder größer',
    level: 'Feld `level` ist gleich `"error"`',
    message: '`message` entspricht dem Regex-Muster',
    boolean: 'boolesche Logik mit Priorität',
    grouping: 'explizite Gruppierung',
  }}
/>

## Unterstützte Sprachen

| Sprache | Parser | In-Memory-Matcher |
|----------|--------|-------------------|
| <TechLabel icon="/icons/go.svg" label="Go" variant="go" /> | Yes | Yes |
| <TechLabel icon="/icons/python.svg" label="Python" /> | Yes | Yes |
| <TechLabel icon="/icons/javascript.svg" label="JavaScript" /> | Yes | Yes |

## SQL-Generatoren

| Datenbank | Go | Python | JavaScript |
|-----------|----|--------|------------|
| <TechLabel icon="/icons/clickhouse_light.svg" label="ClickHouse" variant="db" /> | Yes | Yes | Yes |
| <TechLabel icon="/icons/postgresql.svg" label="PostgreSQL" variant="db" /> | Yes | Yes | Yes |
| <TechLabel icon="/icons/starrocks.svg" label="StarRocks" variant="db" /> | Yes | Yes | Yes |
