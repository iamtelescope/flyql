---
title: Componente de columnas
description: Componente FlyqlColumns para editar expresiones de columnas
---

El componente `FlyqlColumns` ofrece una entrada específica para expresiones de columnas: seleccionar, filtrar y aplicar transformers a columnas.

## Configuración rápida

```vue
<script setup>
import { ref } from 'vue'
import { FlyqlColumns, ColumnSchema } from 'flyql-vue'

const expr = ref('')
const columns = ColumnSchema.fromPlainObject({
    status: { type: 'number', suggest: true },
    level: { type: 'enum', suggest: true },
    service: { type: 'string', suggest: true },
    host: { type: 'string', suggest: true },
})

function onParsed(parsed) {
    console.log('Parsed columns:', parsed)
}
</script>

<template>
    <FlyqlColumns
        v-model="expr"
        :columns="columns"
        :capabilities="{ transformers: true }"
        placeholder="message, status|upper, host as h"
        @update:parsed="onParsed"
    />
</template>
```

## Props

| Prop | Tipo | Valor por defecto | Descripción |
|------|------|-------------------|-------------|
| `modelValue` | `String` | `''` | Texto de la expresión (`v-model`) |
| `columns` | `Object` | `{}` | [Esquema](/es/editor/schema/) de columnas |
| `capabilities` | `Object` | `null` | Capacidades del parser, por ejemplo `{ transformers: true }` |
| `onKeyDiscovery` | `Function` | `null` | Callback para descubrir claves remotas |
| `placeholder` | `String` | `''` | Texto del placeholder |
| `autofocus` | `Boolean` | `false` | Enfoca automáticamente al montar |
| `debug` | `Boolean` | `false` | Logging de depuración |
| `dark` | `Boolean` | `false` | Activa el tema oscuro ([ver temas](/es/editor/theming/)) |
| `label` | `String` | `''` | Texto mostrado dentro del campo, a la izquierda de la consulta |
| `icon` | `String \| Object \| Function \| Boolean` | `null` | Icono mostrado antes de la etiqueta: una cadena se renderiza como texto, un componente tal cual, `false` lo elimina |
| `multiline` | `Boolean` | `true` | Permitir entrada multilínea. Con `false`, los saltos de línea se convierten en espacios y el campo se mantiene en una línea |
| `hasClear` | `Boolean` | `false` | Mostrar un botón de borrado en el extremo final; solo aparece cuando el campo tiene valor |
| `clearButtonLabel` | `String` | `'Clear'` | Nombre accesible del botón de borrado |
| `panelContainer` | `String \| Object` | `'body'` | Dónde se porta el panel de sugerencias: un selector o un elemento. Por defecto `body` |

## Etiqueta e icono

`label` e `icon` comparten el espacio a la izquierda del campo, delante del texto de la consulta. Al hacer clic en cualquiera de los dos se enfoca la entrada, y una etiqueta demasiado larga se trunca con puntos suspensivos a la mitad del ancho del campo en lugar de comprimir la entrada. Un `label` de texto también pasa a ser el nombre accesible de la entrada, reemplazando el `aria-label` predeterminado del componente.

```vue
<template>
    <FlyqlColumns v-model="expr" :columns="columns" label="Filter" />
    <FlyqlColumns v-model="expr" :columns="columns" icon="🔎" />
    <FlyqlColumns v-model="expr" :columns="columns" :icon="MyIcon" />
    <FlyqlColumns v-model="expr" :columns="columns" :icon="false" label="Filter" />
</template>
```

Los slots `icon` y `label` tienen prioridad sobre las props cuando necesitas contenido más rico:

```vue
<template>
    <FlyqlColumns v-model="expr" :columns="columns">
        <template #icon><MyIcon /></template>
        <template #label><strong>Filter</strong></template>
    </FlyqlColumns>
</template>
```

## Eventos

| Evento | Payload | Descripción |
|--------|---------|-------------|
| `update:modelValue` | `string` | El texto de la expresión cambió (`v-model`) |
| `update:parsed` | `ParsedColumn[]` | Se actualizó el array de columnas parseadas |
| `submit` | — | El usuario pulsó Ctrl/Cmd+Enter |
| `parse-error` | `string` | Mensaje de error de parseo |
| `focus` | — | El componente recibió foco |
| `blur` | — | El componente perdió el foco |

## Métodos expuestos

| Método | Devuelve | Descripción |
|--------|----------|-------------|
| `focus()` | — | Enfoca la entrada |
| `blur()` | — | Quita el foco de la entrada |
| `getQueryStatus()` | `{ valid: boolean, message: string }` | Valida la expresión |
| `getParsedColumns()` | `ParsedColumn[]` | Obtiene el array de columnas parseadas |

## Sintaxis de expresiones de columnas

Las expresiones de columnas usan un formato separado por comas con transformers y alias opcionales:

```
message, status|upper, host as h, metadata.labels.env
```

| Parte | Sintaxis | Ejemplo |
|------|----------|---------|
| Columna | `name` | `message` |
| Transformer | `name\|transformer` | `status\|upper` |
| Transformer con argumentos | `name\|transformer(arg)` | `name\|truncate(50)` |
| Alias | `name as alias` | `host as h` |
| Anidada | `parent.child` | `metadata.labels.env` |
| Transformers encadenados | `name\|t1\|t2` | `msg\|lower\|truncate(100)` |
