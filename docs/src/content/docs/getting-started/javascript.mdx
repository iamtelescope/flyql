---
title: JavaScript Quickstart
description: Install and use FlyQL in a JavaScript project
---

import { Code } from '@astrojs/starlight/components'
import parseSnippet from '@snippets/javascript/getting-started-parse.mjs?raw'
import parseErrorSnippet from '@snippets/javascript/getting-started-parse-error.mjs?raw'
import generateSnippet from '@snippets/javascript/getting-started-generate-clickhouse.mjs?raw'
import matchSnippet from '@snippets/javascript/getting-started-match.mjs?raw'
import columnsSnippet from '@snippets/javascript/getting-started-columns.mjs?raw'
import transformersSnippet from '@snippets/javascript/getting-started-transformers.mjs?raw'
import parametersSnippet from '@snippets/javascript/getting-started-parameters.mjs?raw'

## Installation

```bash
npm install flyql
```

**Requirements:** Node.js 16+. All imports use ES modules.

## Parse a Query

<Code code={parseSnippet.trim()} lang="javascript" />

`parse()` returns a `Parser` object with a `root` node representing the AST. It throws a `ParserError` on invalid input:

<Code code={parseErrorSnippet.trim()} lang="javascript" />

## Generate SQL

Use a generator to turn the AST into a WHERE clause. Each database dialect is a separate subpath import.

### ClickHouse

<Code code={generateSnippet.trim()} lang="javascript" />

### Other Dialects

PostgreSQL and StarRocks generators follow the same pattern:

```javascript
import { generateWhere, newColumn } from 'flyql/generators/postgresql'
import { generateWhere, newColumn } from 'flyql/generators/starrocks'
```

Note: PostgreSQL's `newColumn` has a different signature — `newColumn(name, type, values)` (no `jsonString` parameter).

## Match In-Memory

Evaluate a query against a data record without generating SQL:

<Code code={matchSnippet.trim()} lang="javascript" />

## Parse Column Expressions

Parse a columns expression into structured data with segments, transformers, and aliases:

<Code code={columnsSnippet.trim()} lang="javascript" />

## Parameterized Queries

Use `$name` and `$1` placeholders, then resolve them at runtime with `bindParams()`:

<Code code={parametersSnippet.trim()} lang="javascript" />

See [Parameters](/syntax/parameters/) for the full reference.

## Transformers in Queries

Parse a query with transformers and generate SQL:

<Code code={transformersSnippet.trim()} lang="javascript" />

See [Transformers](/syntax/transformers/) for the full syntax reference.

## Available Subpath Imports

| Import Path | Contents |
|-------------|----------|
| `flyql` | Core parser (`parse`, `ParserError`, operators, constants) |
| `flyql/core` | Core parser module directly |
| `flyql/columns` | Column expression parser |
| `flyql/generators/clickhouse` | ClickHouse SQL generator |
| `flyql/generators/postgresql` | PostgreSQL SQL generator |
| `flyql/generators/starrocks` | StarRocks SQL generator |
| `flyql/matcher` | In-memory record matcher |
| `flyql/transformers` | Transformer registry and built-ins |
| `flyql/editor` | Web editor component (requires Vue 3) |
| `flyql/highlight` | Syntax highlighting utilities |

## What's Next

- [Syntax Reference](/syntax/) — all operators, boolean logic, patterns, and more
