---
title: Demarrage rapide Python
description: Installer et utiliser FlyQL dans un projet Python
---

## Installation

FlyQL pour Python s'installe depuis git (non publie sur PyPI) :

```bash
pip install "flyql @ git+https://github.com/iamtelescope/flyql.git#subdirectory=python"
```

Cela installe le package `flyql` ainsi que sa dependance `google-re2`.

**Prerequis :** Python 3.10+

## Analyser une requete

```python
from flyql import parse

result = parse("status = 200 and active")
print(result.root)
```

`parse()` retourne un objet `Parser` avec un noeud `root` representant l'AST. Elle leve une `ParserError` en cas d'entree invalide.

## Generer du SQL

Utilisez un generateur pour transformer l'AST en clause WHERE parametree. Chaque dialecte de base de donnees a son propre generateur.

### ClickHouse

```python
from flyql import parse
from flyql.generators.clickhouse.generator import to_sql_where
from flyql.generators.clickhouse.column import Column

result = parse("status >= 400 and host = prod*")

columns = {
    "status": Column("status", False, "UInt32"),
    "host": Column("host", False, "String"),
}

sql = to_sql_where(result.root, columns)
print(sql)
```

Les generateurs PostgreSQL et StarRocks suivent le meme modele — importez depuis `flyql.generators.postgresql` ou `flyql.generators.starrocks` a la place.

## Matching en memoire

Evaluez une requete par rapport a un enregistrement de donnees sans generer de SQL :

```python
from flyql import parse
from flyql.matcher import Evaluator, Record


result = parse("status = 200 and active")

data = {
    "status": 200,
    "active": True,
    "host": "prod-api-01",
}

evaluator = Evaluator()
matches = evaluator.evaluate(result.root, Record(data))
print(f"Matches: {matches}")  # True
```

## Analyser les expressions de colonnes

Analysez une expression de colonnes en donnees structurees avec segments, transformateurs et alias :

```python
from flyql.columns import parse, parse_to_json

# Parse basic columns (transformers disabled by default)
parsed = parse("message, status")
for col in parsed:
    print(f"{col.name} (display: {col.display_name!r}, segments: {col.segments})")

# Enable transformers via capabilities
with_transforms = parse("message|chars(25) as msg, status", capabilities={"transformers": True})

# Or serialize directly to JSON for API responses
json_str = parse_to_json("message, status|upper", capabilities={"transformers": True})
print(json_str)
```

## Transformateurs dans les requetes

Analysez une requete avec des transformateurs et generez du SQL :

```python
from flyql import parse
from flyql.generators.clickhouse.generator import to_sql_where, Column

# Parse a transformer query
result = parse("message|upper = 'ERROR'")

# Generate SQL for ClickHouse
columns = {"message": Column("message", False, "String")}
sql = to_sql_where(result.root, columns)
print(sql)  # equals(upper(message), 'ERROR')
```

Voir [Transformateurs](/fr/syntax/transformers/) pour la reference complete de la syntaxe.

## Et ensuite

- [Reference de la syntaxe](/fr/syntax/) — tous les operateurs, la logique booleenne, les motifs et plus encore
