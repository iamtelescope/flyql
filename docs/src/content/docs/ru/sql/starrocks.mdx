---
title: StarRocks
description: Примеры генерируемого SQL для StarRocks
---

import FlyqlCode from '../../../../components/FlyqlCode.astro'
import FlyqlToken from '../../../../components/FlyqlToken.astro'

StarRocks использует идентификаторы в обратных кавычках, `regexp()` для регулярных выражений и `->` с JSON-ключами в строковом виде для доступа к JSON.

## Базовые примеры

| FlyQL | Генерируемый SQL |
|-------|------------------|
| <FlyqlCode code={`message = 'hello'`} inline /> | `` `message` = 'hello' `` |
| <FlyqlCode code={`message != 'hello'`} inline /> | `` `message` != 'hello' `` |
| <FlyqlCode code={`count > 10`} inline /> | `` `count` > 10 `` |
| <FlyqlCode code={`count <= 100`} inline /> | `` `count` <= 100 `` |
| <FlyqlCode code={`message ~ 'error.*'`} inline /> | `` regexp(`message`, 'error.*') `` |
| <FlyqlCode code={`message !~ 'test.*'`} inline /> | `` not regexp(`message`, 'test.*') `` |
| <FlyqlCode code={`host = 'prod'`} inline /> | `` `host` = 'prod' `` |

## Логические операторы

| FlyQL | Генерируемый SQL |
|-------|------------------|
| <FlyqlCode code={`a = '1' and b > 10`} inline /> | `` (`a` = '1' and `b` > 10) `` |
| <FlyqlCode code={`a = '1' or b = '2'`} inline /> | `` (`a` = '1' or `b` = '2') `` |
| <FlyqlCode code={`not a = '1'`} inline /> | `` not (`a` = '1') `` |

## Принадлежность к списку

| FlyQL | Генерируемый SQL |
|-------|------------------|
| <FlyqlCode code={`status in [200, 201]`} inline /> | `` `status` IN (200, 201) `` |
| <FlyqlCode code={`env not in ['dev', 'test']`} inline /> | `` `env` NOT IN ('dev', 'test') `` |
| <FlyqlCode code={`status in []`} inline /> | `0` |
| <FlyqlCode code={`status not in []`} inline /> | `1` |

## Вхождение (`has` / `not has`)

Оператор <FlyqlToken type="operator">has</FlyqlToken> генерирует разный SQL в зависимости от типа столбца:

| Тип столбца | FlyQL | Генерируемый SQL |
|-------------|-------|------------------|
| Строка | <FlyqlCode code={`message has 'error'`} inline /> | `` INSTR(`message`, 'error') > 0 `` |
| Строка (отрицание) | <FlyqlCode code={`message not has 'error'`} inline /> | `` (`message` IS NULL OR INSTR(`message`, 'error') = 0) `` |
| Array | <FlyqlCode code={`tags has 'web'`} inline /> | `` array_contains(`tags`, 'web') `` |
| Array (отрицание) | <FlyqlCode code={`tags not has 'web'`} inline /> | `` NOT array_contains(`tags`, 'web') `` |
| Map | <FlyqlCode code={`metadata has 'key'`} inline /> | `` array_contains(map_keys(`metadata`), 'key') `` |
| Map (отрицание) | <FlyqlCode code={`metadata not has 'key'`} inline /> | `` NOT array_contains(map_keys(`metadata`), 'key') `` |
| Нативный JSON | <FlyqlCode code={`data has 'key'`} inline /> | `` json_exists(`data`, concat('$.', 'key')) `` |

## JSON-столбцы

StarRocks поддерживает два типа JSON-столбцов с разным генерируемым SQL.

### Нативный тип JSON

Нативные JSON-столбцы используют оператор `->` с JSON-ключами в строковом виде:

| FlyQL | Генерируемый SQL |
|-------|------------------|
| <FlyqlCode code={`data.name = 'test'`} inline /> | `` `data`->'\"name\"' = 'test' `` |
| <FlyqlCode code={`data.user.name = 'john'`} inline /> | `` `data`->'\"user\"'->'\"name\"' = 'john' `` |

### JSON в виде строки

Столбцы, хранящие JSON как строку, требуют `parse_json()` перед обходом:

| FlyQL | Генерируемый SQL |
|-------|------------------|
| <FlyqlCode code={`data.name = 'test'`} inline /> | `` parse_json(`data`)->'\"name\"' = 'test' `` |
| <FlyqlCode code={`data.user.name = 'john'`} inline /> | `` parse_json(`data`)->'\"user\"'->'\"name\"' = 'john' `` |

## Вложенные сложные типы

Генератор StarRocks трактует сегментированный путь как принадлежащий одному контейнерному типу — он пока не спускается через смешанное вложение вроде `Map(K, Struct)`, `Array(Map)` или `Struct`-поля, которые сами являются `Map`/`Array`. Для таких колонок выравнивайте путь или выносите вложенное значение в отдельную колонку.

## Map-столбцы

Map-столбцы используют скобочную нотацию с прямым сравнением:

| FlyQL | Генерируемый SQL |
|-------|------------------|
| <FlyqlCode code={`metadata.key1 = 'value1'`} inline /> | `` `metadata`['key1'] = 'value1' `` |
| <FlyqlCode code={`metadata.key1 != 'value1'`} inline /> | `` `metadata`['key1'] != 'value1' `` |
| <FlyqlCode code={`metadata.nested.key = 'value'`} inline /> | `` `metadata`['nested']['key'] = 'value' `` |
| <FlyqlCode code={`metadata.pattern ~ 'test.*'`} inline /> | `` `metadata`['pattern'] regexp 'test.*' `` |

## Array-столбцы

Array-столбцы используют скобочную индексацию с нуля:

| FlyQL | Генерируемый SQL |
|-------|------------------|
| <FlyqlCode code={`tags.0 = 'first'`} inline /> | `` `tags`[0] = 'first' `` |
| <FlyqlCode code={`tags.1 = 'second'`} inline /> | `` `tags`[1] = 'second' `` |

### Настройка столбцов

Параметр `jsonString` определяет используемый режим (аналогично ClickHouse):

```go
// Go
columns := map[string]*starrocks.Column{
    "message": starrocks.NewColumn(starrocks.ColumnDef{Name: "message", Type: "VARCHAR(255)"}),
    "data":    starrocks.NewColumn(starrocks.ColumnDef{Name: "data", Type: "JSON"}),      // native JSON
    "log":     starrocks.NewColumn(starrocks.ColumnDef{Name: "log", Type: "jsonstring"}),       // JSON string
}
```

```javascript
// JavaScript
const columns = {
    message: newColumn("message", "VARCHAR(255)"),
    data:    newColumn("data", "JSON"),      // native JSON
    log:     newColumn("log", "jsonstring"),       // JSON string
}
```

## Трансформеры

Трансформеры генерируют стандартные вызовы функций StarRocks с именами столбцов в обратных кавычках:

| FlyQL | Генерируемый SQL |
|-------|------------------|
| <FlyqlCode code={'status\|upper = "ERROR"'} inline /> | `` UPPER(`status`) = 'ERROR' `` |
| <FlyqlCode code={'status\|lower = "info"'} inline /> | `` LOWER(`status`) = 'info' `` |
| <FlyqlCode code={'message\|len > 100'} inline /> | `` LENGTH(`message`) > 100 `` |
| <FlyqlCode code={'message\|lower\|len > 100'} inline /> | `` LENGTH(LOWER(`message`)) > 100 `` |
| <FlyqlCode code={'tags\|split(",") has "urgent"'} inline /> | `` array_contains(SPLIT(`tags`, ','), 'urgent') `` |
