---
title: Documentation
description: A lightweight, injection-proof query language for multi-dialect SQL generation
---

import FlyqlCode from '../../components/FlyqlCode.astro'
import FlyqlQuickExampleTable from '../../components/FlyqlQuickExampleTable.astro'
import TechLabel from '../../components/TechLabel.astro'
import { landingIntroQuery, landingQuickExampleQuery } from '../../data/flyql-examples'

A lightweight query language that transpiles into injection-proof SQL across ClickHouse, PostgreSQL, and StarRocks — with implementations in Go, Python, and JavaScript.

## What is FlyQL?

FlyQL is a simple, human-readable query language designed for filtering data. Write a query like `status >= 400 and host = prod*`, and FlyQL transpiles it into parameterized SQL for your database — or evaluates it in-memory against data records.

<FlyqlCode code={landingIntroQuery} />

This query works identically whether your backend is ClickHouse, PostgreSQL, StarRocks, or an in-memory dataset.

## Key Features

- **Injection-Proof by Design** — The grammar cannot express subqueries, JOINs, UNIONs, or DDL. Security is structural, not defensive. All generated SQL uses parameterized values.
- **Multi-Dialect SQL** — One query language, dialect-specific output. Generates correct SQL for ClickHouse, PostgreSQL, and StarRocks with proper type handling (JSON paths, Map access, Array handling).
- **Multi-Language** — Full implementations in Go, Python, and JavaScript. Parse on the frontend, validate on the backend, generate SQL anywhere — same query, same results.
- **Zero Dependencies** — Hand-written state-machine parser with no external dependencies in any language. Trivial to integrate, nothing to conflict with.

## Quick Example

<FlyqlCode code={landingQuickExampleQuery} />

<FlyqlQuickExampleTable
  componentHeader="Component"
  meaningHeader="Meaning"
  meanings={{
    service: 'service field is not "api"',
    status: 'status is 400 or greater',
    level: 'level field equals "error"',
    message: 'message matches regex pattern',
    boolean: 'boolean logic with precedence',
    grouping: 'explicit grouping',
  }}
/>

## Supported Languages

| Language | Parser | In-Memory Matcher |
|----------|--------|-------------------|
| <TechLabel icon="/icons/go.svg" label="Go" variant="go" /> | Yes | Yes |
| <TechLabel icon="/icons/python.svg" label="Python" /> | Yes | Yes |
| <TechLabel icon="/icons/javascript.svg" label="JavaScript" /> | Yes | Yes |

## SQL Generators

| Database | Go | Python | JavaScript |
|----------|----|--------|------------|
| <TechLabel icon="/icons/clickhouse_light.svg" label="ClickHouse" variant="db" /> | Yes | Yes | Yes |
| <TechLabel icon="/icons/postgresql.svg" label="PostgreSQL" variant="db" /> | Yes | Yes | Yes |
| <TechLabel icon="/icons/starrocks.svg" label="StarRocks" variant="db" /> | Yes | Yes | Yes |
