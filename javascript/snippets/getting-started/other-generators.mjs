// This snippet is display-only reference material.
// The two import lines below shadow the same symbols, so they can't coexist
// in real code — the marker slice below is rendered as docs content only.
// The block-comment wrapper makes this file a valid (empty) module that
// run-snippets.mjs can import without executing the invalid duplicate imports.
/*
// @docs-begin
import { generateWhere, newColumn } from 'flyql/generators/postgresql'
import { generateWhere, newColumn } from 'flyql/generators/starrocks'
// @docs-end
*/
export {}
